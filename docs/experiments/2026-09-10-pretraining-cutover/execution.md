# 首版 base 执行记录

本轮执行依据为 [预训练主计划](../../pretraining-plan.md)。本页记录实际处理与缺口，不替代主计划、运行账本或阶段验收。

## 2026-09-10：首批文本构造

已增加 `python -m minifrontier.data.pretraining`，复用现有 Parquet 范围读取与 corpus 去重构造器。首批候选目标按现有 64K **参考** tokenizer 计数：中文 225M、英文 150M、OpenWebMath 50M、对话 40M。465M 是去重后、划分前的目标，不是已准备的训练 token；代码域仍缺少来源/许可证据，不能用其他域或重复样本补齐。首轮 ≥500M 的去重训练库存尚未完成。

来源版本沿用已核查的中文、FineWeb-Edu、UltraChat 固定提交；[OpenWebMath](https://huggingface.co/datasets/open-web-math/open-web-math) 固定为 `fde8ef8de2300f5e778f56261843dab89f230815`。OpenWebMath 的 ODC-BY、Common Crawl 条款和底层页面权利单独记录。每次远程读取记录文件、文件校验值、行组、行号及采样顺序。

新构造器使用按组 train 98.5% / val 0.5% / test 1%；旧默认划分继续可复现。重复文档的别名组也连接到保留文档，避免重复副本被丢弃后，其同源变体跨划分泄漏。对话保留全部轮次，按 continuation 文本处理。

候选数据输出始终为 `formal_admission=false`。完成时输出实际分来源/划分 token 数、清洗/去重计数和训练集抽查样本；抽查、benchmark 污染排除、最终 tokenizer 与媒体仍需独立证据，不能由构造脚本自动认定通过。现有参考 tokenizer 不等于本轮最终 tokenizer 已冻结。

单个文本构造任务的磁盘上限 12 GiB，新分配保留 80 GiB；使用选中分片，不下载全库。运行状态与源文件哈希保存在该数据版本的 `source-audit.json`。数据原文和抽查样本不进入 Git。

验证：11 项相关 CPU 回归通过；Ruff 和相关模块 mypy 通过；已验证固定中文分片可按范围读取并取得 LFS SHA256。没有为此启动 GPU 测速或训练。

首次构造在 4,073 条、5,851,266 个候选参考 token 后中断。固定文件 `4_5/003932.parquet` 的 3,274 行中，一条 WuDao 子来源记录的 score 为 `1.0126953125`，超出现有读取器声明的 0–1 范围；该文件其余分数中位数约 0.817。修复仅在新候选构造入口将此类异常隔离并记录来源位置/原始分数，不裁剪分数或改写旧实验读取语义。首个未准入候选及失败报告保留，新版本重新构造，不将失败进度算作正式数据或训练量。

## MF1 紧凑二进制消费

`minifrontier mf1 encode --compact` 可生成按完整记录切分的二进制 shard，默认每片约 64M input token。训练器按 manifest 自动选择紧凑加载器；旧 JSONL 与参考导出继续可读。

纯 token 数据由 18 bytes/token（ids、labels、三轴位置）降为 uint16 的约 2.125 bytes/token（ids 加监督位图），另有每记录索引、来源/媒体描述和文件 manifest。超过 65535 的 ID 使用 uint32；编码前验证范围，不能溢出。位置、segment 和模态数组按现有模型规则恢复；原始图片/视频仍解码进入可训练视觉塔，没有缓存视觉 embedding。打开至多四个 shard 的 memmap；采样器建立长度/领域桶时直接读取索引，不重新编码或解码媒体。

验证覆盖跨 shard 的文本/图片/视频、token 与监督掩码、三轴位置、合并 batch 后的逐值相同模型输出、数据损坏拒绝、uint32 范围，以及二进制数据上的暂停/精确恢复。针对这些路径的 7 项测试通过。数据格式通过不代表正式媒体或生产吞吐已准入。

完整 CPU 回归为 362 passed / 1 skipped（常规 357 项及其余 slow/distributed 5 项），Ruff、格式检查及 173 个源文件 mypy 通过。数据构造任务已接入原有实验台账，候选参考 token 单列，不能显示成训练 CE 或正式准入。旧 Qwen batch16 作业已完成 20,016,632 CE；旧三模型 batch16 队列已无运行进程，不派生新实验。这些消耗不计入本轮从零主训练。

## 首批共享视觉候选与代码来源缺口

新增 `python -m minifrontier.data.visual_sources`，复用同一 corpus 构造器和固定版本的 FineVision 范围读取。首批目标为 ALLaVA/LAION 60K 独立图片、CoSyn 文档 20K、图表 20K；同一图的完整问答共享像素文件和划分组，最多保留 8 组完整问答。保留逐问答的原始位置及四项质量评分，四项均至少 3/5 才入候选，不将长答案截断后冒充完整监督。最多读取每子集目标数量 4 倍的源行，达不到目标明确记录缺口。

这些是共享候选图片与 QA 库，不是已经完成的媒体曝光，也不是来源模型的官方训练数据。元数据预算 3 GiB、原始媒体预算 17 GiB；与正在运行的文本构造共同检查 80 GiB 新分配余量。视频、中文 OCR、多图与固定独立视觉验证还需补充；不会把本次图片库当作完整多模态数据验收。

来源条款分别记录：[ALLaVA-4V](https://huggingface.co/datasets/FreedomIntelligence/ALLaVA-4V) 为 CC-BY-NC-4.0，保留底层图像权利与研究使用范围；[CoSyn-400K](https://huggingface.co/datasets/allenai/CoSyn-400K) 声明 ODC-BY-1.0，并说明生成图像/问答的各自条款与研究教育用途。FineVision 的追加提示许可不能覆盖这些来源的条款。源数据卡按实际提交保留本地哈希快照；公开材料不包含源图片、原文或凭据。

代码域核查：现有 6,756 条 Python-Edu 候选只有一条含匹配的 Apache 头部，不能靠文件头补足整个代码域。Python-Edu 的 Parquet 索引确实不含 license/revision_id。The Stack v2 去重版包含这些补充字段，但当前机器凭据读取其文件返回 403；已请求开通数据集访问，文本/视觉准备继续。没有将许可不明的旧 Python 行改标为正式数据。

第一次视觉构造在获取文件目录时遇到远程响应中断，尚未读取图片，失败记录保留。目录读取现对传输错误最多重试三次，权限与 schema 错误仍直接失败；大媒体分片复用已有的并发 HTTP Range 读取器，限制单块大小及每文件网络预算。该修复不更改候选来源、筛选阈值或训练准入条件。

## 数据消费修复与空间回收

MF1 的紧凑加载器进一步修复了跨分片随机采样时反复扫描大文件的问题：完整哈希校验按文件身份复用，采样器只读小索引，媒体描述按偏移映射读取。文件变化仍须重新通过校验，媒体像素校验保持。训练增加数据准备耗时、有效 CE 比例和 padding 比例，见[执行性能审计](../../audits/minifrontier1-execution-performance.md)。相关 34 项测试通过；全仓 CPU 为 370 passed / 1 skipped，Ruff、格式检查和全仓 mypy 通过。这不是新的正式多模态 GPU 吞吐测量。

按主计划回收了 20 个已经结束的旧实验优化器/RNG 检查点，实际删除 51.58 GiB。每项先核对终态及进程引用，再逐值比较保留权重、实际加载保留模型与 tokenizer，删除前再次校验哈希。`model.pt`、最佳权重、tokenizer 和实验报告保留；被回收的旧实验不再提供精确优化器恢复。一个额外候选未通过筛查，完整保留。公开[轻量回收凭据](checkpoint-retirement.json)记录路径、字节数与校验值。

## 工作配方选择与来源模型阶段继承

依据已有 20M 对照和受控 batch 记录，选定[四模型工作参数](working-recipes.md)，完整峰值、全局窗口及主/索引器日程写入现有机器计划。Kimi/DeepSeek 选择 32K、Qwen/MF1 选择 16K；真实数据/tokenizer、阶段长度消费和微批/资源资格尚未最终绑定。没有派发新的科学探索或将旧权重接入正式主线。

新增显式 pretraining program 路径，按参数名与算法契约继承优化器；索引器阶段仅额外保存暂停的主干状态，避免复制活跃优化器。主 CE、router、QK clip、RNG 和兼容的游标连续，阶段账本单列；DeepSeek 收尾 MTP 0.3→0.1 用明确迁移。正式入口要求数据/config/tokenizer/微批绑定，性能报告必须对应同一 program 配方，阶段质量必须绑定实际 `--init` 权重。

小配置实际训练验证了 Kimi 分阶段与连续运行逐值一致、Qwen/DeepSeek 主干状态跨 indexer 保留、暂停/恢复以及 DeepSeek 连续 sparse 阶段的 MTP 切换。机制通过不代表已具备正式数据、稀疏转换质量或生产资格。

首批文本候选已构造完成，374,806 条去重记录按组划分后，训练集 369,181 条、**458,020,690 个参考 token**；其中中文 221,643,597、英文 147,701,678、数学 49,256,193、对话 39,419,222。验证集 1,801 条、测试集 3,824 条；每来源已生成 100 条待人工抽查材料。该切片状态仍为 `candidate_slice_complete_pending_admission`，没有最终正式 tokenizer，代码域及 ≥500M 训练库存目标仍未完成。

视觉原始 PNG 的前 1,000 张已占约 0.78 GiB，原始格式下的全量候选目标超过当前热缓存预算；后续需落实有界媒体存储与消费，不能直接提高本地上限。代码元数据访问复查仍为 403。四模型正式训练均未启动，主 CE 为零。

MF1 又修复了 packed 样本逐 token 读取 GPU segment 标量的问题，固定 packed 文本的完整更新短测提升 17.52%；数值、条件及限制记录在[同一性能审计](../../audits/minifrontier1-execution-performance.md)。正式预训练已配置独立 TensorBoard 日志视图，当前没有正式事件；旧实验与本次测速继续留在原记录中。

## 文档/图表候选与代码补充

自然图片继续在本机按既定上限构造；文档 OCR 与图表另在远端工作盘构造，各目标 20K 图片。远端任务的总上限为 60 GiB（元数据 3 GiB、原始媒体 57 GiB），保留 80 GiB 新写入余量；已实际分配并释放一个 60 GiB 的容量探测文件，核对目录写权限、分配字节数与回收后余量。这证明当时的分配能力，不将共享盘剩余空间视为永久配额保证。本地 25 GiB 热缓存预算保持，跨主机有界消费仍需完成。

文档/图表任务使用冻结源码 `9d3667f`、seed=20260912 与既有固定 FineVision 版本，保存原始图片。首次启动因继承的本机代理端口不可用而在零图片时失败；核对进程已退出和直接 HTTPS 返回 200 后，保留空失败数据/日志，移除该任务的失效代理设置重新启动。第二次启动已实际写入文档图片，不把初始化日志当作下载完成。

The Stack v2 访问仍待处理，旧 Python-Edu 行保持隔离。新增补充候选来自公开 [CodeParrot train](https://huggingface.co/datasets/codeparrot/codeparrot-clean-train)，固定 revision `3e6ab65f2864931e041f6a82db9b5a6ec2b71ab4`。[发布者说明](https://huggingface.co/datasets/codeparrot/codeparrot-clean/discussions/1)将具体许可放在逐文件字段；本项目进一步要求开头注释或模块 docstring 中存在匹配的 Apache-2.0、MIT、BSD-2/3-Clause 声明，拒绝冲突 SPDX、其他许可、vendor/generated 路径和不可解析的 Python 3。许可筛选是候选条件，不能自动授予正式数据或权重发布资格。

候选保留 repo/path、原始内容 SHA256、计算得到的 Git blob SHA1、许可头 hash 和来源分片 hash。原仓库 commit 未由该来源提供，明确记录缺项，不把计算的 blob hash 说成已经向 GitHub 核验过的 blob。源码不执行，不做 NFKC 或空白折叠；代码近重复必须同时保持 AST 一致，防止字符串空格或缩进改变语义后仍被当作相同程序。首次目标为 100M 参考 token，最多扫描 500K 行、下载 3 GiB，内存逐行有界、一次只缓存一片 gzip。实际来源不足时明确报告低于目标，不补重复样本。正式抽查、benchmark 内容排除、词表和跨语料合并仍未通过。

代码构造已从冻结提交 `072ab73241a3e59283be15cd0925d1a812181aa8` 启动，候选目录上限 4 GiB、单片临时下载不超过 300 MiB。该数据改动的全仓 CPU 回归为 383 passed / 1 skipped，54 项 CUDA 未运行；定向来源/语法/去重测试 12 项通过。远端数据的轻量审计已接入原实验台账，远端 PID 不在本机核验；新鲜的远端命令观察标为 `observed_running`，超过 120 秒则恢复未验证状态，候选计数不记入训练 CE。台账定向回归 10 项通过。

## 候选构造中断与保留进度恢复

代码候选在 9,288 条、28,119,941 个参考 token 后遇到超大 JSON 源行；远端文档候选在 244 张独立图片、597 条问答后遇到 CDN 响应正文读取超时。两项中断均保留 SQLite、已落盘媒体和原失败审计，没有重置计数，也没有将候选改为正式准入。

恢复实现提交为 `642aced`。`--resume` 只接受指定来源读取中断，并核对来源版本、seed、目标、参考 tokenizer 与空间上限；视觉恢复先逐文件验证已保留媒体的路径和哈希。恢复前保存原审计、数据库 hash 和处理代码 hash。Parquet 按原采样顺序跳过已完成行组，代码 gzip 按原顺序定位，重新下载的网络流量继续扣预算。超过 2 MiB 的代码源行整体跳过并记拒绝项；不截断为另一条代码样本。单 gzip 的 2 GiB 解压上限保持，触及时记录文件和行号并转到下一分片。

范围读取现对正文传输错误最多重试三次，各次都计入网络字节预算；权限、哈希、schema、范围不符和空间错误不通过传输重试掩盖。恢复后的实际增长已确认：代码超过原中断点并达到 10,000 条、30,442,836 个参考 token，超大源行拒绝计数为 1；远端文档达到 250 张、608 条问答。此处是观察时刻的快照，持续状态以原台账为准，见[恢复记录](candidate-recovery.json)。

完整 CPU 回归为 390 passed / 1 skipped，54 项 CUDA 未运行；18 项数据定向回归通过。恢复测试覆盖保留记录计数、确定性游标、损坏图片拒绝、正文超时重试及网络预算。后续提交 `3c4bc39` 将代码下载的共享磁盘锁缩短为每块落盘期间持有，网络等待不再占锁；该修复的 10 项定向回归通过。当前恢复任务仍使用冻结的 `642aced`，后续启动采用锁修复，不修改活跃任务源码。

四模型正式主 CE 仍均为零。独立 TensorBoard 入口已启动，正式视图没有事件；候选构造、失败恢复和性能验收不写成正式训练曲线。


## 共享文本合并与评测污染筛查完成

本轮共享文本候选合并完成，训练划分保留 **554,952,646 个参考 token**。代码候选此前完成 32,452 条、100,018,868 个参考 token 的构造；合并补充跨切片去重，并保持原验证、测试组留出。实际数量、命令、来源版本和校验值见[文本库存快照](text-inventory.json)。这些数量仍是候选库存，不是正式训练消耗。

评测排除清单固定了 HumanEval、MBPP 和 GSM8K 的 9,930 条来源记录，所有分区均排除于本轮训练。324 条记录按来源身份或文本重合规则被剔除，覆盖 4 个相连组；原始候选及处理记录保留。文件 hash、SQLite 完整性和关联组跨划分检查通过，没有发现跨 split 的相连组。500 条来源/领域分层抽查样本已生成，复核方式与结论尚待确认。

有限样本上的 0.5% 哈希桶未让每个来源的实际验证组比例都达到 0.5%。补充的 75 个验证组现已通过共享分区应用到 tokenizer 与来源模型编码入口，训练库存调整为 **554,830,690 个参考 token**。独立核对确认只将 75 条训练记录、121,956 个参考 token 转入验证集，各来源验证组比例均达到 0.5%；原数据库、历史留出和 500 条抽查样本保持不变，没有跨划分关联组。分区文件共约 9.4 MiB，未复制数据库。

处理代码的完整 CPU 回归为 394 passed / 1 skipped，54 项 CUDA 未运行。首次合并在创建输出前暴露了 JSONL 分行问题：字符串内两处合法 Unicode 分隔符被误当成换行。修复后 5 项定向回归通过，实际 9,930 条清单均可读取；同一输入随后完成合并。修复仅改变读取方式，未改写评测内容或放宽污染匹配条件。


## 正式词表映射准备

分区读取实现提交为 `1d8e380`，完整 CPU 回归 403 passed / 1 skipped，54 项 CUDA 未运行。来源模型 64K 与 MF1 32K 映射已开始从修正后的训练划分构造：固定 sample_id 次序、64 MiB 文本预算、ByteLevel BPE，并各自保留控制 token 表。这是既定词表容量的数据准备，不是新一轮词表规模搜索。运行命令、输入和处理代码 hash 记录在同一数据任务的后续步骤中。

来源模型 64K 与 MF1 32K 词表映射已构造完成，均使用同一份 67,107,100 字节训练文本。两套映射各自通过固定 500 条训练样本的 round-trip、控制 token 和 ID 范围检查；耗时分别约 63.7 秒和 58.1 秒。词表文件 hash 和逐来源切分计数已收入[文本库存快照](text-inventory.json)。这些机械检查不代替来源质量复核，映射仍未标记正式冻结。质量结论、完整编码和模型数据准入尚待完成，四个模型正式 CE 均为零；6008 正式面板仍无训练事件。

首次词表准备在产出映射前失败：tokenizers 将迭代器分批交给不同 Rayon 线程，触发 SQLite 连接的线程限制。修复提交 `80e64a5` 将受 64 MiB 预算限制的训练文本先在连接所属线程读完，再交给分词器；新增跨工作线程消费回归，18 项数据定向测试通过。原语料与分区未重做，失败日志及空产物审计保留，第二次构造采用修复后的固定源码，随后完成了两套映射及上述机械检查。


## 共享文本编码与 MF1 连续文档窗口

实现提交 `44bbf67` 接通共享分区到 MF1 紧凑编码。每篇完整文本只编码和存储一次，保留来源与 split group；阶段采样按当前 context 读取连续窗口，边界仅重叠一个上下文 token，第一位置不计 CE，文档内部不插入 EOS。样本中的控制 token 字面量沿用 MF1 转义规则。只有纯文本 continuation 可以这样处理，原生图文问答仍保持完整。

采样状态增加每个领域正在读取的文档及其偏移。真实 CPU 更新回归验证了文档中途暂停后模型、优化器、路由、RNG、采样状态与 CE 账本均与连续训练一致；验证读取也覆盖每个文档的全部 CE 位置一次。完整 CPU 回归 408 passed / 1 skipped，54 项 CUDA 未运行。这些是数据路径证据，不是 MF1 生产图文吞吐或能力资格。

来源模型共享的 64K 文本编码与 MF1 32K 紧凑文本编码均已完成首次写入，磁盘上限分别为 11 GiB、3 GiB。两者共同读取已落实的文本分区，保留不同映射与实际 CE 统计。全量核验结果及来源模型编码修复见下节；MF1 文本仍只是联合数据的一部分。处理记录作为原数据任务的后续步骤保存，未派发新的 GPU 探索实验。


## 全量编码核验与自然图像切片

全量审计提交 `a316c5a` 对两套编码逐篇检查文档覆盖、文件哈希、划分、来源、文档边界、监督掩码、ID 范围和正文解码。MF1 的 **406,934 篇文档全部通过机械检查**；训练、验证、测试实际 CE 分别为 **579,472,905 / 3,045,747 / 6,586,010**。来源质量复核尚未完成，因此没有把机械检查自动升级为正式数据准入。

来源模型的首次编码有 **1 篇训练文档**将正文中的控制符字面量识别为协议 token，其余全量检查没有报错；该编码不予准入。修复 `7d7530a` 使用 tokenizers 的普通文本编码设置，保留原文字节往返一致，只由写入器加入 BOS/EOS；这个设置与 `add_special_tokens` 的后处理开关不同，见 [tokenizers 官方 API 定义](https://github.com/huggingface/tokenizers/blob/main/bindings/python/py_src/tokenizers/__init__.pyi)。新增测试覆盖全部控制符及旧写入行为的失败检测，8 项 MF1 文本检查与 14 项语料/分区检查通过。既有 tokenizer 映射和共享分区保持不变。

确认没有训练消费者和活跃编码/审计进程后，已回收错误版本的 **7,379,171,309 字节（约 6.87 GiB）**派生文件；原 manifest、词表、失败报告、文件大小与哈希清单保留。新的 `data/pretraining-text-64k-v2` 已开始构造，完成写入后自动执行相同全量审计。公开数量与状态见[文本库存快照](text-inventory.json)，旧失败产物不计入可用训练库存。

自然图像构造此前在 17 GiB 原图上限处停止。通过 `51802e8` 的切片固化入口，已保留 **23,260 张原图、43,542 条完整问答**，没有新增下载或修改像素；这没有完成原定 60K 自然图像目标。独立核验覆盖所有原图文件哈希、数据库及关联身份，跨划分相连组、来源别名和 RGB 图像身份均为零。训练/验证/测试分别有 **22,938 / 111 / 211** 张原图，训练答案量为 **6,850,027 个参考 token**；这里不是 MF1 实际 CE，更不是模型训练消耗。分层抽查保留 caption/VQA 各 100 条，结论仍待复核。

MF1 真实图像编码提交 `713eb78` 将 canonical 问题、完整答案与原图身份映射为 typed messages，复用紧凑 shard 和原始像素。P0 按专项方案选择 **max_features=49、不开文档 tiles**，不保存视觉 embedding；编码清单另列完整记录在各 context 桶中的实际 CE。11 项媒体/紧凑格式回归通过，覆盖答案 mask、原图哈希、split、加载前后模型输出、损坏媒体拒绝和超长答案拒绝；Ruff、格式检查及模块 mypy 通过。保留自然图像切片上的实际 P0 编码已启动，预算 1 GiB，仍待联合数据及阶段准入。

远端文档已达到 20K 张目标，图表继续构造；准确观察时间、当前数量与限制见[媒体库存快照](media-inventory.json)。当前自然图像留出量不足完整视觉验收要求，OCR/中文 OCR、多图、视频和上游视觉评测身份排除仍有缺口。四模型正式主 CE 仍均为零，正式 TensorBoard 6008 保持独立。

完成上述修复与媒体接入后，完整 CPU 回归为 **422 passed / 1 skipped**，54 项 CUDA 未运行，耗时 138.31 秒；全仓库 Ruff 检查、318 个文件格式检查及 178 个源码文件 mypy 均通过。此次检查没有运行模型能力评估，也没有产生可计入正式主预算的权重。


## 文本核验完成、MF1 联合消费与性能入口

来源模型的修复编码已完成全量核验，40.05 万篇训练文档保留 **542,124,865 个实际 CE token**，验证和测试分别为 **2,851,102 / 6,182,019 CE**；正文控制符异常已消除。原文、分区与 tokenizer 映射保持不变，修复使训练 CE 比错误版本增加 1。两套文本编码的文件及计数证据见[文本库存快照](text-inventory.json)，来源质量审核仍待完成。

MF1 的自然图像 P0 编码覆盖全部 **43,542 条**候选问答；训练部分 **42,937 条、7,455,654 CE**，对应 **22,938 张独立原图**。其中 VQA 为 5,543,841 CE、caption 为 1,911,813 CE。训练记录中 87 条完整图文问答需要 1024 context，其余均能放进 512；没有截短这些答案。编码约 80.23 MiB，继续引用同一批原图。

图像审计 `fe58029` 逐条核对来源、划分、控制符、完整答案掩码、媒体 span 与几何信息，并核对所有原图文件 hash。全量审计通过；新增负例验证即使更新了文件 hash，错误 BOS 监督和偏移错误仍会被拒绝。此处检查的是数据编码，不构成来源质量、视觉能力或生产吞吐验收。

联合加载实现 `c4414a2` 采用稳定索引组合现有紧凑组件，保留各组件的 domain 表、完整文本游标和原图根路径；不重编码或复制 token shard。实际联合清单只新增 **2,488,427 字节（约 2.37 MiB）**，训练库存为 400,480 篇文本加 42,937 条图文问答。各组件 manifest 通过哈希绑定，组合时检查重复 sample、跨划分关联组与图像身份。当前组合缺 OCR/文档、图表，仍标记为部分 P0 数据。

`a2bad9c` 增加 `minifrontier mf1 benchmark`：复用 P0 的 CE 混合、512/1024 长度、packing、主 CE 学习率日程、loss/MTP、优化器与路由更新。默认 50 次性能预热、200 次测量，总计最多 250 次更新；16K global input 的完整更新上界为 **4,351,750 input token**。报告同时保存全部更新的 CE/input 账本和测量窗口的吞吐、context、显存与 LR。性能预热不替代 2M 主 CE 的 LR warmup。此入口没有 init/resume，也不导出 checkpoint；能力/正式准入保持 false，台账将其明确归为 performance，消耗不补计主训练。

数据和配方冻结后才启动上述生产测量。本轮只做 CPU 回归，没有派发 GPU 性能任务。既有 acceptance 保持 2M 上限，并补充执行中的实际 token 检查；到达上限前拒绝未训练的超预算窗口，保留之前的恢复点。完整 CPU 回归为 **437 passed / 1 skipped**、54 项 CUDA 未运行，耗时 144.61 秒；Ruff、319 个文件格式及 179 个源码文件 mypy 检查通过。其后图像审计的 27 项文本/媒体定向回归通过。

远端文档/图表构造的存储观察发现：`CorpusBuilder` 的 `4 × payload + 1024` 估算没有覆盖实际感知哈希关联索引，数据库及附属元数据已超过配置的 3 GiB 子预算。实际总量仍低于该任务的 60 GiB 上限，远端余量超过 1,000 GiB；该项已标记存储未准入，最终清单需核对实际表/索引占用及重复关联并纠正计量。不得把估算计数写成实际空间验收通过，详见[媒体库存快照](media-inventory.json)。


## 文档／图表库存完成与存储修复

远端候选构造已完成 **20,000 张文档图像 + 20,000 张图表图像、159,195 条完整问答**；原始媒体共 11,090,765,087 字节。原始划分为 train/val/test 157,236 / 682 / 1,277 条问答，独立原图 39,496 / 169 / 335 张；实际关联组为 33,169 / 157 / 325。数量达到本切片目标，来源质量、上游视觉评测身份排除及模型准入仍未完成。

只读数据库检查确认 10,690,404 条关联中有 **9,015,609 条重复关系**。`5e36c4e` 对新数据库使用 `(key,id)` 唯一复合主键并取消额外 rowid/键索引，旧候选通过显式新版本压缩；实现参考 [SQLite WITHOUT ROWID 文档](https://www.sqlite.org/withoutrowid.html)。构造估算补计媒体关联及索引，并检查真实数据库页量；视觉 finalization 另按实际元数据及回滚日志峰值核对预算。

实际 `pretraining-media-doc-chart-v2` 数据库为 **1,159,458,816 字节（约 1.08 GiB）**，比旧版 3,674,583,040 字节小约 68.4%。全部样本、两个去重索引表、唯一关联集合逐表双向比对通过，原文、来源、分组和划分均保持；完整原图哈希检查通过，原图采用硬链接共享。压缩目的库设物理页数上限，副本与原库重叠期间仍在原 60 GiB 总预算内。旧版暂时保留，因此此处报告的是新版本体积下降，**没有把差额记成已释放磁盘空间**。旧版 3 GiB 子预算超额记录也继续保留。

完整 CPU 回归 **446 passed / 1 skipped**，54 项 CUDA 未运行，耗时 141.99 秒；179 个源码文件 mypy 通过。`d3ac05c` 进一步让 MF1 图像编码与全量审计消费有效分区视图，并从原数据库定位共享像素；视图统计同步重算图像数、关联组与答案参考 token。38 项定向回归通过，涵盖新增留出组不会进入训练、原验证／测试保持、像素不复制及真实编码审计。

文档／图表的原始验证划分只有 157 个独立关联组。按主计划 §5.3，在保护既有 val/test 和 200 条固定人工抽查材料的前提下，以固定 SHA256 顺序额外预留完整训练组，目标为每来源至少 500 组、全局至少 1,000 组，再生成 P0 紧凑编码并进行全量编码审计。此举明确增加初始小切片的验证比例，不把原 0.5% 哈希划分宣称已满足 1,000 组。实际已额外预留 **843 个完整训练组**，文档／图表验证集达到 **1,000 组、1,061 张原图**；新划分的训练／验证／测试原图为 38,604 / 1,061 / 335 张，测试保持不变。有效分区没有复制数据库或像素。MF1 P0 编码已开始，计划上限 1 GiB，随后自动执行全量编码审计；实际记录见[媒体库存快照](media-inventory.json)。

执行中保留两项辅助脚本失败：系统 Python 不支持 `hashlib.file_digest`，已改为有界流式 SHA256；较旧构造源码的 audit 未保存分划分答案 token，已改从不可变样本计算并与原有逐来源总量核对。两次均未产生正式训练或改写原始数据。四模型正式主 CE 仍为零，人工质量审核、最终数据／配方及生产准入仍待完成。
