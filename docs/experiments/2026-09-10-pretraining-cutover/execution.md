# 首版 base 执行记录

本轮执行依据为 [预训练主计划](../../pretraining-plan.md)。本页记录实际处理与缺口，不替代主计划、运行账本或阶段验收。

## 2026-09-10：首批文本构造

已增加 `python -m minifrontier.data.pretraining`，复用现有 Parquet 范围读取与 corpus 去重构造器。首批候选目标按现有 64K **参考** tokenizer 计数：中文 225M、英文 150M、OpenWebMath 50M、对话 40M。465M 是去重后、划分前的目标，不是已准备的训练 token；代码域仍缺少来源/许可证据，不能用其他域或重复样本补齐。首轮 ≥500M 的去重训练库存尚未完成。

来源版本沿用已核查的中文、FineWeb-Edu、UltraChat 固定提交；[OpenWebMath](https://huggingface.co/datasets/open-web-math/open-web-math) 固定为 `fde8ef8de2300f5e778f56261843dab89f230815`。OpenWebMath 的 ODC-BY、Common Crawl 条款和底层页面权利单独记录。每次远程读取记录文件、文件校验值、行组、行号及采样顺序。

新构造器使用按组 train 98.5% / val 0.5% / test 1%；旧默认划分继续可复现。重复文档的别名组也连接到保留文档，避免重复副本被丢弃后，其同源变体跨划分泄漏。对话保留全部轮次，按 continuation 文本处理。

候选数据输出始终为 `formal_admission=false`。完成时输出实际分来源/划分 token 数、清洗/去重计数和训练集抽查样本；抽查、benchmark 污染排除、最终 tokenizer 与媒体仍需独立证据，不能由构造脚本自动认定通过。现有参考 tokenizer 不等于本轮最终 tokenizer 已冻结。

单个文本构造任务的磁盘上限 12 GiB，新分配保留 80 GiB；使用选中分片，不下载全库。运行状态与源文件哈希保存在该数据版本的 `source-audit.json`。数据原文和抽查样本不进入 Git。

验证：11 项相关 CPU 回归通过；Ruff 和相关模块 mypy 通过；已验证固定中文分片可按范围读取并取得 LFS SHA256。没有为此启动 GPU 测速或训练。

首次构造在 4,073 条、5,851,266 个候选参考 token 后中断。固定文件 `4_5/003932.parquet` 的 3,274 行中，一条 WuDao 子来源记录的 score 为 `1.0126953125`，超出现有读取器声明的 0–1 范围；该文件其余分数中位数约 0.817。修复仅在新候选构造入口将此类异常隔离并记录来源位置/原始分数，不裁剪分数或改写旧实验读取语义。首个未准入候选及失败报告保留，新版本重新构造，不将失败进度算作正式数据或训练量。

## MF1 紧凑二进制消费

`minifrontier mf1 encode --compact` 可生成按完整记录切分的二进制 shard，默认每片约 64M input token。训练器按 manifest 自动选择紧凑加载器；旧 JSONL 与参考导出继续可读。

纯 token 数据由 18 bytes/token（ids、labels、三轴位置）降为 uint16 的约 2.125 bytes/token（ids 加监督位图），另有每记录索引、来源/媒体描述和文件 manifest。超过 65535 的 ID 使用 uint32；编码前验证范围，不能溢出。位置、segment 和模态数组按现有模型规则恢复；原始图片/视频仍解码进入可训练视觉塔，没有缓存视觉 embedding。打开至多四个 shard 的 memmap；采样器建立长度/领域桶时直接读取索引，不重新编码或解码媒体。

验证覆盖跨 shard 的文本/图片/视频、token 与监督掩码、三轴位置、合并 batch 后的逐值相同模型输出、数据损坏拒绝、uint32 范围，以及二进制数据上的暂停/精确恢复。针对这些路径的 7 项测试通过。数据格式通过不代表正式媒体或生产吞吐已准入。

完整 CPU 回归为 362 passed / 1 skipped（常规 357 项及其余 slow/distributed 5 项），Ruff、格式检查及 173 个源文件 mypy 通过。数据构造任务已接入原有实验台账，候选参考 token 单列，不能显示成训练 CE 或正式准入。旧 Qwen batch16 作业已完成 20,016,632 CE；旧三模型 batch16 队列已无运行进程，不派生新实验。这些消耗不计入本轮从零主训练。

## 首批共享视觉候选与代码来源缺口

新增 `python -m minifrontier.data.visual_sources`，复用同一 corpus 构造器和固定版本的 FineVision 范围读取。首批目标为 ALLaVA/LAION 60K 独立图片、CoSyn 文档 20K、图表 20K；同一图的完整问答共享像素文件和划分组，最多保留 8 组完整问答。保留逐问答的原始位置及四项质量评分，四项均至少 3/5 才入候选，不将长答案截断后冒充完整监督。最多读取每子集目标数量 4 倍的源行，达不到目标明确记录缺口。

这些是共享候选图片与 QA 库，不是已经完成的媒体曝光，也不是来源模型的官方训练数据。元数据预算 3 GiB、原始媒体预算 17 GiB；与正在运行的文本构造共同检查 80 GiB 新分配余量。视频、中文 OCR、多图与固定独立视觉验证还需补充；不会把本次图片库当作完整多模态数据验收。

来源条款分别记录：[ALLaVA-4V](https://huggingface.co/datasets/FreedomIntelligence/ALLaVA-4V) 为 CC-BY-NC-4.0，保留底层图像权利与研究使用范围；[CoSyn-400K](https://huggingface.co/datasets/allenai/CoSyn-400K) 声明 ODC-BY-1.0，并说明生成图像/问答的各自条款与研究教育用途。FineVision 的追加提示许可不能覆盖这些来源的条款。源数据卡按实际提交保留本地哈希快照；公开材料不包含源图片、原文或凭据。

代码域核查：现有 6,756 条 Python-Edu 候选只有一条含匹配的 Apache 头部，不能靠文件头补足整个代码域。Python-Edu 的 Parquet 索引确实不含 license/revision_id。The Stack v2 去重版包含这些补充字段，但当前机器凭据读取其文件返回 403；已请求开通数据集访问，文本/视觉准备继续。没有将许可不明的旧 Python 行改标为正式数据。

第一次视觉构造在获取文件目录时遇到远程响应中断，尚未读取图片，失败记录保留。目录读取现对传输错误最多重试三次，权限与 schema 错误仍直接失败；大媒体分片复用已有的并发 HTTP Range 读取器，限制单块大小及每文件网络预算。该修复不更改候选来源、筛选阈值或训练准入条件。

## 数据消费修复与空间回收

MF1 的紧凑加载器进一步修复了跨分片随机采样时反复扫描大文件的问题：完整哈希校验按文件身份复用，采样器只读小索引，媒体描述按偏移映射读取。文件变化仍须重新通过校验，媒体像素校验保持。训练增加数据准备耗时、有效 CE 比例和 padding 比例，见[执行性能审计](../../audits/minifrontier1-execution-performance.md)。相关 34 项测试通过；全仓 CPU 为 370 passed / 1 skipped，Ruff、格式检查和全仓 mypy 通过。这不是新的正式多模态 GPU 吞吐测量。

按主计划回收了 20 个已经结束的旧实验优化器/RNG 检查点，实际删除 51.58 GiB。每项先核对终态及进程引用，再逐值比较保留权重、实际加载保留模型与 tokenizer，删除前再次校验哈希。`model.pt`、最佳权重、tokenizer 和实验报告保留；被回收的旧实验不再提供精确优化器恢复。一个额外候选未通过筛查，完整保留。公开[轻量回收凭据](checkpoint-retirement.json)记录路径、字节数与校验值。

## 工作配方选择与来源模型阶段继承

依据已有 20M 对照和受控 batch 记录，选定[四模型工作参数](working-recipes.md)，完整峰值、全局窗口及主/索引器日程写入现有机器计划。Kimi/DeepSeek 选择 32K、Qwen/MF1 选择 16K；真实数据/tokenizer、阶段长度消费和微批/资源资格尚未最终绑定。没有派发新的科学探索或将旧权重接入正式主线。

新增显式 pretraining program 路径，按参数名与算法契约继承优化器；索引器阶段仅额外保存暂停的主干状态，避免复制活跃优化器。主 CE、router、QK clip、RNG 和兼容的游标连续，阶段账本单列；DeepSeek 收尾 MTP 0.3→0.1 用明确迁移。正式入口要求数据/config/tokenizer/微批绑定，性能报告必须对应同一 program 配方，阶段质量必须绑定实际 `--init` 权重。

小配置实际训练验证了 Kimi 分阶段与连续运行逐值一致、Qwen/DeepSeek 主干状态跨 indexer 保留、暂停/恢复以及 DeepSeek 连续 sparse 阶段的 MTP 切换。机制通过不代表已具备正式数据、稀疏转换质量或生产资格。

首批文本候选已构造完成，374,806 条去重记录按组划分后，训练集 369,181 条、**458,020,690 个参考 token**；其中中文 221,643,597、英文 147,701,678、数学 49,256,193、对话 39,419,222。验证集 1,801 条、测试集 3,824 条；每来源已生成 100 条待人工抽查材料。该切片状态仍为 `candidate_slice_complete_pending_admission`，没有最终正式 tokenizer，代码域及 ≥500M 训练库存目标仍未完成。

视觉原始 PNG 的前 1,000 张已占约 0.78 GiB，原始格式下的全量候选目标超过当前热缓存预算；后续需落实有界媒体存储与消费，不能直接提高本地上限。代码元数据访问复查仍为 403。四模型正式训练均未启动，主 CE 为零。

MF1 又修复了 packed 样本逐 token 读取 GPU segment 标量的问题，固定 packed 文本的完整更新短测提升 17.52%；数值、条件及限制记录在[同一性能审计](../../audits/minifrontier1-execution-performance.md)。正式预训练已配置独立 TensorBoard 日志视图，当前没有正式事件；旧实验与本次测速继续留在原记录中。

## 文档/图表候选与代码补充

自然图片继续在本机按既定上限构造；文档 OCR 与图表另在远端工作盘构造，各目标 20K 图片。远端任务的总上限为 60 GiB（元数据 3 GiB、原始媒体 57 GiB），保留 80 GiB 新写入余量；已实际分配并释放一个 60 GiB 的容量探测文件，核对目录写权限、分配字节数与回收后余量。这证明当时的分配能力，不将共享盘剩余空间视为永久配额保证。本地 25 GiB 热缓存预算保持，跨主机有界消费仍需完成。

文档/图表任务使用冻结源码 `9d3667f`、seed=20260912 与既有固定 FineVision 版本，保存原始图片。首次启动因继承的本机代理端口不可用而在零图片时失败；核对进程已退出和直接 HTTPS 返回 200 后，保留空失败数据/日志，移除该任务的失效代理设置重新启动。第二次启动已实际写入文档图片，不把初始化日志当作下载完成。

The Stack v2 访问仍待处理，旧 Python-Edu 行保持隔离。新增补充候选来自公开 [CodeParrot train](https://huggingface.co/datasets/codeparrot/codeparrot-clean-train)，固定 revision `3e6ab65f2864931e041f6a82db9b5a6ec2b71ab4`。[发布者说明](https://huggingface.co/datasets/codeparrot/codeparrot-clean/discussions/1)将具体许可放在逐文件字段；本项目进一步要求开头注释或模块 docstring 中存在匹配的 Apache-2.0、MIT、BSD-2/3-Clause 声明，拒绝冲突 SPDX、其他许可、vendor/generated 路径和不可解析的 Python 3。许可筛选是候选条件，不能自动授予正式数据或权重发布资格。

候选保留 repo/path、原始内容 SHA256、计算得到的 Git blob SHA1、许可头 hash 和来源分片 hash。原仓库 commit 未由该来源提供，明确记录缺项，不把计算的 blob hash 说成已经向 GitHub 核验过的 blob。源码不执行，不做 NFKC 或空白折叠；代码近重复必须同时保持 AST 一致，防止字符串空格或缩进改变语义后仍被当作相同程序。首次目标为 100M 参考 token，最多扫描 500K 行、下载 3 GiB，内存逐行有界、一次只缓存一片 gzip。实际来源不足时明确报告低于目标，不补重复样本。正式抽查、benchmark 内容排除、词表和跨语料合并仍未通过。

代码构造已从冻结提交 `072ab73241a3e59283be15cd0925d1a812181aa8` 启动，候选目录上限 4 GiB、单片临时下载不超过 300 MiB。该数据改动的全仓 CPU 回归为 383 passed / 1 skipped，54 项 CUDA 未运行；定向来源/语法/去重测试 12 项通过。远端数据的轻量审计已接入原实验台账，远端 PID 不在本机核验；新鲜的远端命令观察标为 `observed_running`，超过 120 秒则恢复未验证状态，候选计数不记入训练 CE。台账定向回归 10 项通过。

## 候选构造中断与保留进度恢复

代码候选在 9,288 条、28,119,941 个参考 token 后遇到超大 JSON 源行；远端文档候选在 244 张独立图片、597 条问答后遇到 CDN 响应正文读取超时。两项中断均保留 SQLite、已落盘媒体和原失败审计，没有重置计数，也没有将候选改为正式准入。

恢复实现提交为 `642aced`。`--resume` 只接受指定来源读取中断，并核对来源版本、seed、目标、参考 tokenizer 与空间上限；视觉恢复先逐文件验证已保留媒体的路径和哈希。恢复前保存原审计、数据库 hash 和处理代码 hash。Parquet 按原采样顺序跳过已完成行组，代码 gzip 按原顺序定位，重新下载的网络流量继续扣预算。超过 2 MiB 的代码源行整体跳过并记拒绝项；不截断为另一条代码样本。单 gzip 的 2 GiB 解压上限保持，触及时记录文件和行号并转到下一分片。

范围读取现对正文传输错误最多重试三次，各次都计入网络字节预算；权限、哈希、schema、范围不符和空间错误不通过传输重试掩盖。恢复后的实际增长已确认：代码超过原中断点并达到 10,000 条、30,442,836 个参考 token，超大源行拒绝计数为 1；远端文档达到 250 张、608 条问答。此处是观察时刻的快照，持续状态以原台账为准，见[恢复记录](candidate-recovery.json)。

完整 CPU 回归为 390 passed / 1 skipped，54 项 CUDA 未运行；18 项数据定向回归通过。恢复测试覆盖保留记录计数、确定性游标、损坏图片拒绝、正文超时重试及网络预算。后续提交 `3c4bc39` 将代码下载的共享磁盘锁缩短为每块落盘期间持有，网络等待不再占锁；该修复的 10 项定向回归通过。当前恢复任务仍使用冻结的 `642aced`，后续启动采用锁修复，不修改活跃任务源码。

四模型正式主 CE 仍均为零。独立 TensorBoard 入口已启动，正式视图没有事件；候选构造、失败恢复和性能验收不写成正式训练曲线。


## 共享文本合并与评测污染筛查完成

本轮共享文本候选合并完成，训练划分保留 **554,952,646 个参考 token**。代码候选此前完成 32,452 条、100,018,868 个参考 token 的构造；合并补充跨切片去重，并保持原验证、测试组留出。实际数量、命令、来源版本和校验值见[文本库存快照](text-inventory.json)。这些数量仍是候选库存，不是正式训练消耗。

评测排除清单固定了 HumanEval、MBPP 和 GSM8K 的 9,930 条来源记录，所有分区均排除于本轮训练。324 条记录按来源身份或文本重合规则被剔除，覆盖 4 个相连组；原始候选及处理记录保留。文件 hash、SQLite 完整性和关联组跨划分检查通过，没有发现跨 split 的相连组。500 条来源/领域分层抽查样本已生成，复核方式与结论尚待确认。

有限样本上的 0.5% 哈希桶未让每个来源的实际验证组比例都达到 0.5%。补充的 75 个验证组现已通过共享分区应用到 tokenizer 与来源模型编码入口，训练库存调整为 **554,830,690 个参考 token**。独立核对确认只将 75 条训练记录、121,956 个参考 token 转入验证集，各来源验证组比例均达到 0.5%；原数据库、历史留出和 500 条抽查样本保持不变，没有跨划分关联组。分区文件共约 9.4 MiB，未复制数据库。

处理代码的完整 CPU 回归为 394 passed / 1 skipped，54 项 CUDA 未运行。首次合并在创建输出前暴露了 JSONL 分行问题：字符串内两处合法 Unicode 分隔符被误当成换行。修复后 5 项定向回归通过，实际 9,930 条清单均可读取；同一输入随后完成合并。修复仅改变读取方式，未改写评测内容或放宽污染匹配条件。


## 正式词表映射准备

分区读取实现提交为 `1d8e380`，完整 CPU 回归 403 passed / 1 skipped，54 项 CUDA 未运行。来源模型 64K 与 MF1 32K 映射已开始从修正后的训练划分构造：固定 sample_id 次序、64 MiB 文本预算、ByteLevel BPE，并各自保留控制 token 表。这是既定词表容量的数据准备，不是新一轮词表规模搜索。运行命令、输入和处理代码 hash 记录在同一数据任务的后续步骤中。

来源模型 64K 与 MF1 32K 词表映射已构造完成，均使用同一份 67,107,100 字节训练文本。两套映射各自通过固定 500 条训练样本的 round-trip、控制 token 和 ID 范围检查；耗时分别约 63.7 秒和 58.1 秒。词表文件 hash 和逐来源切分计数已收入[文本库存快照](text-inventory.json)。这些机械检查不代替来源质量复核，映射仍未标记正式冻结。质量结论、完整编码和模型数据准入尚待完成，四个模型正式 CE 均为零；6008 正式面板仍无训练事件。

首次词表准备在产出映射前失败：tokenizers 将迭代器分批交给不同 Rayon 线程，触发 SQLite 连接的线程限制。修复提交 `80e64a5` 将受 64 MiB 预算限制的训练文本先在连接所属线程读完，再交给分词器；新增跨工作线程消费回归，18 项数据定向测试通过。原语料与分区未重做，失败日志及空产物审计保留，第二次构造采用修复后的固定源码，随后完成了两套映射及上述机械检查。


## 共享文本编码与 MF1 连续文档窗口

实现提交 `44bbf67` 接通共享分区到 MF1 紧凑编码。每篇完整文本只编码和存储一次，保留来源与 split group；阶段采样按当前 context 读取连续窗口，边界仅重叠一个上下文 token，第一位置不计 CE，文档内部不插入 EOS。样本中的控制 token 字面量沿用 MF1 转义规则。只有纯文本 continuation 可以这样处理，原生图文问答仍保持完整。

采样状态增加每个领域正在读取的文档及其偏移。真实 CPU 更新回归验证了文档中途暂停后模型、优化器、路由、RNG、采样状态与 CE 账本均与连续训练一致；验证读取也覆盖每个文档的全部 CE 位置一次。完整 CPU 回归 408 passed / 1 skipped，54 项 CUDA 未运行。这些是数据路径证据，不是 MF1 生产图文吞吐或能力资格。

来源模型共享的 64K 文本编码与 MF1 32K 紧凑文本编码均已完成首次写入，磁盘上限分别为 11 GiB、3 GiB。两者共同读取已落实的文本分区，保留不同映射与实际 CE 统计。全量核验结果及来源模型编码修复见下节；MF1 文本仍只是联合数据的一部分。处理记录作为原数据任务的后续步骤保存，未派发新的 GPU 探索实验。


## 全量编码核验与自然图像切片

全量审计提交 `a316c5a` 对两套编码逐篇检查文档覆盖、文件哈希、划分、来源、文档边界、监督掩码、ID 范围和正文解码。MF1 的 **406,934 篇文档全部通过机械检查**；训练、验证、测试实际 CE 分别为 **579,472,905 / 3,045,747 / 6,586,010**。来源质量复核尚未完成，因此没有把机械检查自动升级为正式数据准入。

来源模型的首次编码有 **1 篇训练文档**将正文中的控制符字面量识别为协议 token，其余全量检查没有报错；该编码不予准入。修复 `7d7530a` 使用 tokenizers 的普通文本编码设置，保留原文字节往返一致，只由写入器加入 BOS/EOS；这个设置与 `add_special_tokens` 的后处理开关不同，见 [tokenizers 官方 API 定义](https://github.com/huggingface/tokenizers/blob/main/bindings/python/py_src/tokenizers/__init__.pyi)。新增测试覆盖全部控制符及旧写入行为的失败检测，8 项 MF1 文本检查与 14 项语料/分区检查通过。既有 tokenizer 映射和共享分区保持不变。

确认没有训练消费者和活跃编码/审计进程后，已回收错误版本的 **7,379,171,309 字节（约 6.87 GiB）**派生文件；原 manifest、词表、失败报告、文件大小与哈希清单保留。新的 `data/pretraining-text-64k-v2` 已开始构造，完成写入后自动执行相同全量审计。公开数量与状态见[文本库存快照](text-inventory.json)，旧失败产物不计入可用训练库存。

自然图像构造此前在 17 GiB 原图上限处停止。通过 `51802e8` 的切片固化入口，已保留 **23,260 张原图、43,542 条完整问答**，没有新增下载或修改像素；这没有完成原定 60K 自然图像目标。独立核验覆盖所有原图文件哈希、数据库及关联身份，跨划分相连组、来源别名和 RGB 图像身份均为零。训练/验证/测试分别有 **22,938 / 111 / 211** 张原图，训练答案量为 **6,850,027 个参考 token**；这里不是 MF1 实际 CE，更不是模型训练消耗。分层抽查保留 caption/VQA 各 100 条，结论仍待复核。

MF1 真实图像编码提交 `713eb78` 将 canonical 问题、完整答案与原图身份映射为 typed messages，复用紧凑 shard 和原始像素。P0 按专项方案选择 **max_features=49、不开文档 tiles**，不保存视觉 embedding；编码清单另列完整记录在各 context 桶中的实际 CE。11 项媒体/紧凑格式回归通过，覆盖答案 mask、原图哈希、split、加载前后模型输出、损坏媒体拒绝和超长答案拒绝；Ruff、格式检查及模块 mypy 通过。保留自然图像切片上的实际 P0 编码已启动，预算 1 GiB，仍待联合数据及阶段准入。

远端文档已达到 20K 张目标，图表继续构造；准确观察时间、当前数量与限制见[媒体库存快照](media-inventory.json)。当前自然图像留出量不足完整视觉验收要求，OCR/中文 OCR、多图、视频和上游视觉评测身份排除仍有缺口。四模型正式主 CE 仍均为零，正式 TensorBoard 6008 保持独立。

完成上述修复与媒体接入后，完整 CPU 回归为 **422 passed / 1 skipped**，54 项 CUDA 未运行，耗时 138.31 秒；全仓库 Ruff 检查、318 个文件格式检查及 178 个源码文件 mypy 均通过。此次检查没有运行模型能力评估，也没有产生可计入正式主预算的权重。


## 文本核验完成、MF1 联合消费与性能入口

来源模型的修复编码已完成全量核验，40.05 万篇训练文档保留 **542,124,865 个实际 CE token**，验证和测试分别为 **2,851,102 / 6,182,019 CE**；正文控制符异常已消除。原文、分区与 tokenizer 映射保持不变，修复使训练 CE 比错误版本增加 1。两套文本编码的文件及计数证据见[文本库存快照](text-inventory.json)，来源质量审核仍待完成。

MF1 的自然图像 P0 编码覆盖全部 **43,542 条**候选问答；训练部分 **42,937 条、7,455,654 CE**，对应 **22,938 张独立原图**。其中 VQA 为 5,543,841 CE、caption 为 1,911,813 CE。训练记录中 87 条完整图文问答需要 1024 context，其余均能放进 512；没有截短这些答案。编码约 80.23 MiB，继续引用同一批原图。

图像审计 `fe58029` 逐条核对来源、划分、控制符、完整答案掩码、媒体 span 与几何信息，并核对所有原图文件 hash。全量审计通过；新增负例验证即使更新了文件 hash，错误 BOS 监督和偏移错误仍会被拒绝。此处检查的是数据编码，不构成来源质量、视觉能力或生产吞吐验收。

联合加载实现 `c4414a2` 采用稳定索引组合现有紧凑组件，保留各组件的 domain 表、完整文本游标和原图根路径；不重编码或复制 token shard。实际联合清单只新增 **2,488,427 字节（约 2.37 MiB）**，训练库存为 400,480 篇文本加 42,937 条图文问答。各组件 manifest 通过哈希绑定，组合时检查重复 sample、跨划分关联组与图像身份。当前组合缺 OCR/文档、图表，仍标记为部分 P0 数据。

`a2bad9c` 增加 `minifrontier mf1 benchmark`：复用 P0 的 CE 混合、512/1024 长度、packing、主 CE 学习率日程、loss/MTP、优化器与路由更新。默认 50 次性能预热、200 次测量，总计最多 250 次更新；16K global input 的完整更新上界为 **4,351,750 input token**。报告同时保存全部更新的 CE/input 账本和测量窗口的吞吐、context、显存与 LR。性能预热不替代 2M 主 CE 的 LR warmup。此入口没有 init/resume，也不导出 checkpoint；能力/正式准入保持 false，台账将其明确归为 performance，消耗不补计主训练。

数据和配方冻结后才启动上述生产测量。本轮只做 CPU 回归，没有派发 GPU 性能任务。既有 acceptance 保持 2M 上限，并补充执行中的实际 token 检查；到达上限前拒绝未训练的超预算窗口，保留之前的恢复点。完整 CPU 回归为 **437 passed / 1 skipped**、54 项 CUDA 未运行，耗时 144.61 秒；Ruff、319 个文件格式及 179 个源码文件 mypy 检查通过。其后图像审计的 27 项文本/媒体定向回归通过。

远端文档/图表构造的存储观察发现：`CorpusBuilder` 的 `4 × payload + 1024` 估算没有覆盖实际感知哈希关联索引，数据库及附属元数据已超过配置的 3 GiB 子预算。实际总量仍低于该任务的 60 GiB 上限，远端余量超过 1,000 GiB；该项已标记存储未准入，最终清单需核对实际表/索引占用及重复关联并纠正计量。不得把估算计数写成实际空间验收通过，详见[媒体库存快照](media-inventory.json)。


## 文档／图表库存完成与存储修复

远端候选构造已完成 **20,000 张文档图像 + 20,000 张图表图像、159,195 条完整问答**；原始媒体共 11,090,765,087 字节。原始划分为 train/val/test 157,236 / 682 / 1,277 条问答，独立原图 39,496 / 169 / 335 张；实际关联组为 33,169 / 157 / 325。数量达到本切片目标，来源质量、上游视觉评测身份排除及模型准入仍未完成。

只读数据库检查确认 10,690,404 条关联中有 **9,015,609 条重复关系**。`5e36c4e` 对新数据库使用 `(key,id)` 唯一复合主键并取消额外 rowid/键索引，旧候选通过显式新版本压缩；实现参考 [SQLite WITHOUT ROWID 文档](https://www.sqlite.org/withoutrowid.html)。构造估算补计媒体关联及索引，并检查真实数据库页量；视觉 finalization 另按实际元数据及回滚日志峰值核对预算。

实际 `pretraining-media-doc-chart-v2` 数据库为 **1,159,458,816 字节（约 1.08 GiB）**，比旧版 3,674,583,040 字节小约 68.4%。全部样本、两个去重索引表、唯一关联集合逐表双向比对通过，原文、来源、分组和划分均保持；完整原图哈希检查通过，原图采用硬链接共享。压缩目的库设物理页数上限，副本与原库重叠期间仍在原 60 GiB 总预算内。旧版暂时保留，因此此处报告的是新版本体积下降，**没有把差额记成已释放磁盘空间**。旧版 3 GiB 子预算超额记录也继续保留。

完整 CPU 回归 **446 passed / 1 skipped**，54 项 CUDA 未运行，耗时 141.99 秒；179 个源码文件 mypy 通过。`d3ac05c` 进一步让 MF1 图像编码与全量审计消费有效分区视图，并从原数据库定位共享像素；视图统计同步重算图像数、关联组与答案参考 token。38 项定向回归通过，涵盖新增留出组不会进入训练、原验证／测试保持、像素不复制及真实编码审计。

文档／图表的原始验证划分只有 157 个独立关联组。按主计划 §5.3，在保护既有 val/test 和 200 条固定人工抽查材料的前提下，以固定 SHA256 顺序额外预留完整训练组，目标为每来源至少 500 组、全局至少 1,000 组，再生成 P0 紧凑编码并进行全量编码审计。此举明确增加初始小切片的验证比例，不把原 0.5% 哈希划分宣称已满足 1,000 组。实际已额外预留 **843 个完整训练组**，文档／图表验证集达到 **1,000 组、1,061 张原图**；新划分的训练／验证／测试原图为 38,604 / 1,061 / 335 张，测试保持不变。有效分区没有复制数据库或像素。MF1 P0 编码已开始，计划上限 1 GiB，随后自动执行全量编码审计；实际记录见[媒体库存快照](media-inventory.json)。

执行中保留两项辅助脚本失败：系统 Python 不支持 `hashlib.file_digest`，已改为有界流式 SHA256；较旧构造源码的 audit 未保存分划分答案 token，已改从不可变样本计算并与原有逐来源总量核对。两次均未产生正式训练或改写原始数据。四模型正式主 CE 仍为零，人工质量审核、最终数据／配方及生产准入仍待完成。


## 固定 CE 验证与阶段末文本库存

主计划要求定期约 1M、阶段末约 5M CE；此前来源模型仍按样本数／更新数验证，门槛却统一要求每次 5M。`54498be` 增加显式 `--pretraining-eval`，采用固定无放回样本选择，完整 context 窗口累计到 1M／5M CE，最后一个完整窗口的超额单列；按域合并 microbatch，并逐批核对标签产生的实际 CE。定期验证按累计主 CE 前 100M 每 10M、之后每 50M 触发，跨阶段／恢复沿用同一主 CE 轴。策略、实际选择索引 hash 和分域计数绑定恢复配方。阶段末结果与定期子集分开，不能据两者 NLL 大小比较选 best。

正式 main 阶段门槛分别要求 1M／5M，并强制启用上述执行路径；运行前检查真实验证库存达到阶段末预算，没有删除阶段末要求。MF1 原生 evaluator 尚待同样对齐，此来源模型开关不改变 MF1 的验证行为。实际 CPU 集成检查了 CE 加权分域 NLL、训练状态及 RNG 的精确恢复，并拒绝不同采样 seed 的恢复。完整 CPU 回归 **451 passed / 1 skipped**，54 项 CUDA 未运行，耗时 146.69 秒；没有运行新的 GPU 诊断或计入正式主预算。

旧有效验证集在两套词表下分别为 2,851,102 / 3,045,747 CE，不能满足阶段末 5M。追加分区从未留出的训练组中，按固定 SHA256 顺序选择 **1,624 个完整组、1,673 篇文档**，保持旧 val/test、此前 75 组补充留出及 500 条抽查材料。还保护原 tokenizer 训练涉及的 10,743 个组和字节预算停止边界；分区应用前后重新计算其训练字节，仍为 **67,107,100 字节**且 SHA256 相同，词表没有重训或换映射。

`pretraining-text-partition-v2` 已实际生成，train/val/test 为 **398,807 / 3,735 / 4,392 篇**。按此前逐文档已核验编码计数，新 val 为来源模型 **5,065,923 CE**、MF1 **5,416,788 CE**。这明确增加初始小切片的验证比例，测试未参与配方选择。两套消费者的重编码及全量核验已启动，验证结束前不把新编码标成已准入。每套编码都需再次核对完整原文、mask、CE 和测试分片不变；详见[文本库存快照](text-inventory.json)。

新旧 token 缓存过渡按 **8.6 GiB 临时写入上界**登记，来源编码上限 7.1 GiB、MF1 编码 1.5 GiB；主工作站启动前剩余约 143 GiB，高于扣除预约后的 80 GiB 保留线。原缓存等新版本完整通过并迁移消费者后，再依赖核对并退役派生 payload；当前没有把预计释放量计成已释放。canonical 原文和媒体没有为此复制。四个正式 base 仍未启动，质量审核、最终配方及生产准入继续保留为未完成。

### MF1 固定验证预算与新编码消费检查

MF1 正式 P0/P1/P2/P3 已使用主计划的固定验证规则：首 100M 主 CE 每 10M 验证，之后每 50M；常规约 1M、阶段末约 5M。紧凑数据直接读取监督位图建立固定集合，文本窗口的 CE 不重叠，原生媒体答案保持完整；按领域合并 microbatch，核对实际 CE 与每域分母。策略、seed、上下文、注意力模式和选中窗口校验值进入恢复身份；不足 5M 验证库存不能开始正式主阶段。暂停使用常规集合，同一步已完成的验证不重复执行，恢复后的后续频率仍依据累计主 CE。

同时修复 P2 稠密／稀疏混合训练后验证沿用最后一个 microbatch 注意力模式的问题：验证固定为该阶段声明的模式，并在成功或失败后恢复原训练状态，不修改参数的可训练标记。TensorBoard 常规曲线保留 `eval/*`，阶段末使用 `eval/phase_end_*`，两者分别显示域 CE 和置黑媒体分母。完整 CPU 回归 **456 通过、1 跳过、54 项 CUDA 未运行**；类型检查覆盖 180 个源码文件，格式与静态检查通过。此轮没有新增 GPU 配方实验；生产 GPU 上的验证耗时比例和完整 1,000 组视觉能力评价仍未验收。

两套新文本编码的全量审计均已通过：各检查 **406,934 条完整文本**，封存测试集保持原有字节。来源模型／MF1 的训练库存分别为 **539,910,044 / 577,101,864 CE**，验证库存分别为 **5,065,923 / 5,416,788 CE**。1024 上下文、seed 42 的固定常规／阶段末集合分别为来源模型 **1,000,696 / 5,000,771 CE**，MF1 **1,000,034 / 5,000,347 CE**；预算尾部保留完整窗口，超出量均少于一个窗口。选择与计数检查没有执行模型前向；编码审计通过也不代表质量准入或 tokenizer 已正式冻结。

MF1 的文本＋自然图像组合已迁移到新文本分区，采用原 shard 引用，仅新增约 **2.49 MB** 元数据和 tokenizer 副本。组合训练库存为 **584,557,518 CE**、验证库存为 **5,451,947 CE**，仍缺 OCR／图表组件及正式媒体供给准入。核对新编码审计、封存测试集、活动进程和组合依赖后，旧两套编码的派生文件已按文件校验值记录并删除，释放约 **8.23 GiB**；保留旧 manifest、tokenizer、审计与退役记录，原始语料和模型权重未删除。原组合保留为历史记录并明确标记替代版本。磁盘余量恢复至约 **143.09 GiB**；四模型正式主 CE 仍均为零。

### Kimi / Qwen 原生自然图像编码

`20d484d` 修复 canonical 图文预训练时，原文控制字符串可能被误识别为结构 token 的问题。图像占位符由媒体结构插入，问题／答案中的同名控制字符串按普通文本保留；继续使用本项目来源模型的全文 next-token 监督，媒体标记与视觉 feature 不计入 CE。这个具体 mask 是项目实现选择，不能据此声称复现上游未公开的逐 token 数据配方。原始图片及解码像素校验、完整问题／答案、分组划分、标签与 CE 均进入逐条编码审计。

原生编码可引用已核验的共享文本，避免 Kimi、Qwen 各复制一套约 7 GiB 的文本文件；新增写入有明确字节上限。Qwen 在紧像素预算下向上取整可能越过 49 个 feature，已在真实 resize 前处理边界。完整 CPU 回归 **465 通过、1 跳过、54 项 CUDA 未运行**，类型检查覆盖 180 个文件；这轮检查包含真实像素的微型模型前向／反向，但没有执行生产 GPU 性能验收。

两路自然图像编码已按冻结源码启动：共同读取现有 **23,260 张图、43,542 条 QA**，共享 64K 文本编码，最长 1024 input token，Kimi 最多 64 个视觉 feature，Qwen 最多 49 个；每路新增文件上限 **1 GiB**，保留 **80 GiB** 可用空间。超长样本整条拒绝并计数，不截断答案。编码后自动全量核对，原图和文本 payload 均不复制。当前状态与校验值见[媒体库存快照](media-inventory.json)。自然图像组件尚不能满足正式阶段全部模态与暴露量，质量审核及生产准入仍未完成；独立 TensorBoard 6008 仅在正式训练写出事件后显示曲线。

### 自然图像增量供给

首片因存储上限停在 23,260 张，未达到原定 60,000 张目标。`c969730` 增加已关闭切片的读取位置与像素排除清单导出：新切片继续相同 revision、文件／行组／行的确定顺序，并在落盘前排除全部旧图的 RGB hash。存储中断时重读最后一条尚未保留的边界记录；传输恢复使用“切片起点＋本片已读行数”，不能重新从源头消费。清单包含父 corpus、原数据库、tokenizer 和源码校验值；后续导出累积排除历史切片的图片。跨切片近重复、相连组及评测污染仍需单独核对。

在辅助工作站启动 **新增 36,740 张**自然图片的候选构造，固定 ALLaVA/FineVision 原版本与 seed，从第 **23,790** 个已跳过的源记录之后继续读取。新切片总上限 **40 GiB**，其中元数据 **4 GiB**；同机现有数据约 **17.44 GiB**，另为正在执行的 MF1 文档／图表编码预约 1 GiB，总规划低于该机本轮数据的 60 GiB 上限，实际磁盘余量约 **1,035 GiB**。没有将这批原图复制到主工作站。达到图片目标、最多 146,960 条新增源记录、存储上限或有限传输重试耗尽即停止，实际数量由台账报告。

针对性检查 **27 项通过**，覆盖跨片旧图排除、原切片不变、连续切片游标、存储边界、传输中断恢复以及源文件清单变化时拒绝继续；类型与静态检查通过。新增供给仍是候选，尚未完成质量和模型阶段准入，不计正式训练 CE。

### 跨库媒体分组与训练记录隔离

`2cb2ca6` 从两侧不可变数据库导出 RGB hash、pHash、有效划分及保留的同源别名，仅传输约 **25.64 MB** 身份元数据。完整检查 **23,260 张自然图像＋40,000 张文档／图表**，没有跨库 RGB 完全重复；按现有 ≤6 bit pHash 保守分组规则找到 **85 条近似连接、4 个跨训练／留出的相连组**。近似连接不等于已确认的语义重复，但不能让同一保守分组同时参与训练与留出评价。

`63ec034` 增加只读训练组隔离，并保持已有验证／测试成员及先前新增的 1,000 个文档／图表验证组。实际从训练视图隔离自然图像侧 **5 组、8 张图、13 条 QA**，文档／图表侧 **2 组、2 张图、4 条 QA**；原数据库、原图和旧产物均保留。新视图为 `pretraining-natural-partition-v1` 与辅助工作站上的 `pretraining-doc-chart-partition-v2`，训练 QA 分别为 **42,924 / 153,636**。两侧原有抽查样本没有替换；质量审核状态仍为待完成。

对新视图重新导出并完整分组后，有效媒体 **63,250 张**、独立相连组 **56,744 个**，跨训练／留出冲突归零；另有 **78 条同划分近似连接**，保留其分组信息。此检查未覆盖正在构造的增量切片，也不替代外部视觉 benchmark 身份核查。完整 CPU 回归 **470 通过、1 跳过、54 项 CUDA 未运行**，耗时 149.90 秒；类型检查覆盖 181 个源码文件，格式及静态检查通过。

已登记全部既有媒体编码消费者的强制迁移：原编码只对应原始切片，不能在未应用上述隔离时准入。Kimi、Qwen 原生编码已进入全量审计；MF1 文档／图表编码继续执行，完成后复用已核验 token 并过滤被隔离的训练记录。该次观察时编码消费者和 MF1 组合数据尚未迁移；其后迁移结果见下节，canonical 分组修复本身不构成正式数据准入。同期增量切片已实际记录新增 **2,500 张图、4,680 条 QA**；后续还需纳入同样的跨片核对。四模型正式 CE 仍为零。


### 媒体编码隔离结果与消费者迁移

四路媒体编码和 MF1 文本＋自然图像组合已实际迁移。`130a53f` 复用父编码的完整审计，逐文件检查 hash，按完整训练组移除记录，并重新核算 CE、曝光和媒体边界；保留记录的 token／标签／媒体元数据不变，验证／测试 payload 的文件校验值不变。MF1 重打包紧凑数值数据；Kimi／Qwen 保留原 JSON 记录字节，只重新处理被剔除记录以核对扣减量，没有重做全部图像编码。

| 新编码 | 保留训练 QA | 可用训练 CE | 验证／测试 |
|---|---:|---:|---|
| MF1 自然图像 v2 | 42,924 | 7,453,551 | 原文件字节保持不变 |
| Kimi 自然图像 v2 | 42,924 | 8,060,931 | 原文件字节保持不变 |
| Qwen 自然图像 v2 | 42,924 | 8,060,931 | 原文件字节保持不变 |
| MF1 文档／图表 v2 | 153,636 | 6,887,646 | 原文件字节保持不变 |

Kimi／Qwen 使用同一文本 tokenizer 与预训练监督规则，视觉 feature 数不同，因此这里 CE 相同、input 数不同。MF1 使用自身 tokenizer 和答案监督，不能直接横比数值大小。以上均为可消费库存，不是已经训练的 token。

MF1 组合 `pretraining-mf1-p0-components-v3` 仅新增约 **2.49 MB** 元数据，训练库存为 **584,555,415 CE**；1024 上下文、seed 42 的固定验证成员、窗口和域分母全部保持不变，常规／阶段末仍为 **1,000,974 / 5,000,766 CE**。该本地组合仍缺远端 OCR／图表接入，后续通过有界缓存或在数据所在机器执行，不能直接复制全部原图越过本地缓存预算。

远端首次过滤因原审计使用 `encoding_audit_sha256` 字段，在写输出前停止。`75745ef` 兼容这份已有的 hash 绑定，没有改写父审计；保留失败记录后完成第二次执行。针对性检查 **35 通过**，仓库 CI 类型检查命令覆盖 **182 个源码文件**并通过，格式／静态检查通过。四套新编码与组合共新增 **909,782,752 字节，约 0.85 GiB**，原图及共享文本 payload 没有复制；观察时主工作站剩余 **141.75 GiB**，辅助工作站约 **1,029.36 GiB**。

当前完整迁移身份、校验值、各域 CE 和扣减数见[媒体库存快照](media-inventory.json)。自然图像增量切片已记录 **7,250 张图、13,557 条 QA**，仍在构造，需在关闭后进行跨片核对。来源质量复核、外部视觉 benchmark 身份排除、tokenizer 冻结、各阶段供给与生产准入仍未完成；四模型正式 CE 均为零，独立 TensorBoard 6008 尚无训练事件。


### MF1 四域组件与远端原图缓存

`eb0c590` 为现有紧凑组件增加有界原图缓存，保留编码格式和采样顺序。文档／图表组件的 **260,477,142 字节**编码已从辅助工作站传入，全部文件逐一核对大小及 SHA256；原图继续由原机器保留。图片服务仅监听回环地址，训练机器通过 SSH 转发访问。缓存上限 **6 GiB**、单文件 **64 MiB**、新写入保留 **80 GiB** 可用空间，验证图片持久保留，只驱逐可恢复的训练缓存。

实际组装 `pretraining-mf1-p0-components-v4`，接入共享文本、自然图像、OCR／文档和图表。训练库存为 **591,443,061 CE**，其中视觉答案 **14,341,197 CE**、训练原图 **61,532 张**；此处是数据库存，正式训练计数仍为零。新增组合文件约 **2.49 MB**，没有复制文本 shard。

已预取 **1,061 张**文档／图表验证图片，耗时 **30.86 秒**。随后比较两域各 8 条训练、4 条验证样本，共 **24 条真实 QA**；缓存读取与原机器直接解码得到的 token、labels、像素张量和 grid 校验值全部一致，未执行模型前向。当前缓存 **304,836,187 字节，约 291 MiB**，含 1,061 张保留验证图和 16 张训练图。

新组合加入文档／图表留出数据，1024 上下文、seed 42 的固定验证集合相应重新生成：常规 **1,000,373 CE**，阶段末 **5,000,244 CE**；完整 val 库存为 **5,642,613 CE**。它是新组合开训前的绑定，不把新增领域后的集合称为与旧组合相同。共享接口的完整 CPU 回归 **483 通过、1 跳过、54 项 CUDA 未运行**；最后的读取缓冲与索引页上限调整又通过 **29 项**定向复验，183 个源码文件的类型检查通过。

以下为任务分类修正前的库存快照，caption／VQA 数值已由下节替代。P0 的视觉预算仍为 **40M CE**，当时各域库存为 caption **1,911,515**、OCR／文档 **1,471,510**、VQA **5,542,036**、图表 **5,416,136 CE**。按既定 35%／35%／20%／10% 配方，caption 和 OCR 的新增库存需求最明显，不能因加载器跑通而将数据标为正式准入。自然图像增量构造已记录 **10,750 张图、20,077 条 QA**，进程仍在运行。后续重点是补齐实际供给、来源质量与生产资格；完整记录和缓存界限见[媒体库存快照](media-inventory.json)。


### 自然图像任务分类修正

旧关键词分类漏掉了一部分标准 caption 提示，并误收了少量包含 details 等词的内容型问答。`ee45a28` 改为匹配 [ALLaVA 固定版本的 15 个 caption 模板](https://github.com/FreedomIntelligence/ALLaVA/blob/896b77f0ac48c95031cf31ce1b95ccaa380e68bf/prompts/instructions_for_captions.txt)；分发代码只保存规范化模板的 SHA256。新只读视图 `pretraining-natural-partition-v2` 保留全部 43,529 条有效 QA、原图和分区成员，修正训练集 **13,183 条 VQA→caption、636 条 caption→VQA**，验证／测试也按同一规则修正任务标签。

`1625d13` 将修正应用到 MF1、Kimi、Qwen 的 natural 编码 v3，并组装 `pretraining-mf1-p0-components-v5`。每条已有 token、loss mask、媒体引用和顺序均保持；原数据与父审计不改写。三份编码和组合共增加 **649,415,724 字节，约 0.60 GiB**，未复制原图或共享文本。当前训练库存按修正后的任务统计如下：

| 模型 / 范围 | caption CE | VQA CE | 合计 CE |
|---|---:|---:|---:|
| MF1 自然图像 | 4,752,753 | 2,700,798 | 7,453,551 |
| Kimi 自然图像 | 4,805,519 | 3,255,412 | 8,060,931 |
| Qwen 自然图像 | 4,805,519 | 3,255,412 | 8,060,931 |

MF1 四域组合总库存仍为 **591,443,061 CE**。按 P0 既定比例且不靠重复曝光补足，caption、OCR／文档、VQA 仍分别缺 **9,247,247 / 12,528,490 / 5,299,202 CE**；图表库存超过当前目标。旧 caption／VQA 库存及缺口仅保留为修正前记录，不能继续用于配方供给决策。固定验证的成员、窗口、常规／阶段末 CE 数保持不变，领域分母与绑定随任务标签更新；不宣称验证／测试文件字节相同。

[FineVision 固定版本的数据卡](https://huggingface.co/datasets/HuggingFaceM4/FineVision/blob/3c380a731a3429c1d04693d6ec16d7e683def84c/README.md)列出 CoSyn 文档子集 **71,282** 条原始训练图片记录。当前短答案文档 QA 不能替代整页 OCR 转写，也不能据“CoSyn-400K”名称规划 200K 张独立文档；后续需补充有来源的完整 OCR 数据。该差异是供给缺口，不通过重复少量样本填平。

针对性检查 **68 通过**，完整 CPU 回归 **488 通过、1 跳过、54 项 CUDA 未运行**；最后补充生产者的分类器依赖校验值后另有 **22 项**通过。类型检查覆盖 **184 个源码文件**，静态与格式检查通过。观察时本地剩余 **140.52 GiB**、辅助工作站约 **1,023.55 GiB**；自然图像增量已记录 **15,250 张图、28,581 条 QA**，需关闭后应用同一分类规则并核对跨片重复。人工质量审核、外部视觉污染核查、tokenizer 冻结、供给与生产资格仍待完成。正式 CE 仍为零；TensorBoard **6008** 独立运行且尚无训练曲线。


### 基础 OCR 首批构造与模型编码

`366822b` 新增确定性的中英文 OCR 构造器，`d63f55d` 将原文身份与划分检查接入 MF1 组件组合和来源模型编码。按 P0 的 **224×224、49 个 MF1 视觉 feature** 生成可完整读出的短文本图片；字号为 18／20／22，标签来自实际画入图片的完整行。参考 [SynthDoG 的有来源文本渲染思路](https://github.com/clovaai/donut/tree/master/synthdog)，实现使用本项目的短文本渲染器；不把它称为高分辨率整页 OCR。原文来自已固定、已划分的中英文教育语料，保留来源版本、许可、原文身份和截取位置。训练、验证、测试分别使用不重叠的字体文件与版式族；字体和条款作为共享运行时资源保存，不进入源码或 wheel。

首批四个不相交的原文 ID 区间 `[0,1)`、`[1,2)`、`[2,3)`、`[3,4)` 已完成，共 **76,676 张图／条转写**：训练 **75,173**、验证 **712**、测试 **791**。每片图片与原文的划分不一致数均为 **0**。原图共 **1,293,004,901 字节**，含数据库等文件共 **1,773,943,412 字节**；四路 CPU 构造预算合计最多 16 GiB，写入保留 80 GiB 可用空间。复用原文明确计入跨模态曝光，不增加独立文本库存。

首片保留 19,535 条记录，三套完整编码与机械检查均已完成：

| 模型 | 训练记录 | 训练 input token | 训练 CE | 图像 feature |
|---|---:|---:|---:|---:|
| MF1 | 19,205 | 2,104,169 | 711,087 | 每图 49 |
| Kimi | 19,205 | 2,480,382 | 1,097,622 | 每图 64 |
| Qwen | 19,205 | 2,096,282 | 1,097,622 | 每图 49 |

MF1 使用已有的答案监督，Kimi／Qwen 使用已有的问答全文预训练目标；视觉 feature 不计主 CE。词表与 loss mask 不同，因此上表 CE 不是跨模型效果比较。首片编码尚未接入当前 MF1 v5 组合，不能直接加到其已公布的库存里。跨片精确转写核对发现 1 条重复内容，两条均在训练集；跨池 RGB、原始组及 pHash 检查的结果和后续处理写入[媒体库存快照](media-inventory.json)。

每片准备中英文各 100 条人工复核材料，目前尚未完成人工审核。24 张训练图片的模型辅助可读性检查仅用于工程检查，不代替该审核。新增及相关路径 **50 项针对性测试通过**，185 个源码文件类型检查和静态／格式检查通过；上一节的完整 CPU 回归早于 OCR 变更，不作为本次新增代码的完整回归结果。正式数据准入、外部视觉污染核查和阶段供给仍待完成；四模型正式 CE 仍为零。


#### OCR 版式近似误连的修正

初次跨池检查覆盖 **139,926 张图**，没有重复 RGB，但 pHash 单独连出 **38 个跨划分组件**，最大组件包含 17,505 个原始组。检查其中 8 对两组冲突样本：pHash 距离为 4 或 6，实际转写不同，规范化五字片段 Jaccard 均为 0。[ImageHash 的说明](https://github.com/JohannesBuchner/imagehash)将这类哈希描述为亮度结构特征；结合这批实际样本，不能将 OCR 的相似排版直接认作相同文字。这只是对所检查 8 对的结论，不代表其他候选已全部排除。

`f9dac4d` 保留原始组、来源别名和精确 RGB 检查，为生成 OCR 加入实际转写的哈希片段。跨片文字近重复采用规范化、casefold 后的五字片段 Jaccard≥0.85，候选通过共同排序前缀查找，再计算完整集合相似度；与字体、版式和 pHash 无关。来源已知而没有文字指纹的旧 OCR 身份导出必须重新生成，旧自然图片身份格式仍受支持。OCR 与自然图片／文档的 pHash 候选仍明确列为待核对项，有待核对项时不发布通过状态。现有留出成员和源数据库均未改写，第一次检查的证据也继续保留。

针对该问题的 **30 项**检查通过，包括同文不同字体、异文相同版式、外部图片未核对时拒绝放行，以及与暴力枚举比较文字近重复查找的边界。完整 CPU 回归 **493 通过、1 跳过、54 项 CUDA 未运行**，耗时 **155.04 秒**；185 个源码文件类型检查、静态及格式检查通过。三条 CPU 编码队列已启动，分别处理其余三个关闭分片的 MF1、Kimi、Qwen 编码，合计新增文件上限 9 GiB。所有产物继续标为候选；最终分组、核对与实际编码进度见同页媒体库存快照。


修正后的全量检查覆盖相同的 139,926 张图：已有来源／RGB 分组、自然图片 pHash 和生成 OCR 文字相似关系中未发现跨划分冲突，另保留 **455 对生成 OCR 与外部图片的 pHash 候选**。其中 35 对跨划分、420 对同划分。35 对涉及 10 张外部图片，已校验原文件及解码 RGB 并逐对查看；可见内容不同，记录为模型辅助视觉重复核对，不替代人工来源审核，也不证明已排除语义改写或外部 benchmark 污染。420 对尚未核对，因此总分组状态仍未通过。

MF1、Kimi、Qwen 的四片 OCR 编码及完整机械检查已全部完成。MF1 四片合计可消费训练记录 **75,173**、主 CE **2,779,628**；Kimi／Qwen 各为 **4,294,121 CE**。这些是候选库存，尚未接入 v5 组合或正式训练。辅助工作站的自然图片增量达到 **26,000 张、48,653 条 QA**，已核对仍为原进程运行；关闭后仍需修正任务分类与进行跨片核对。图文原件、复核联系图和样本正文继续保留在忽略目录中，公开记录只包含统计、方法与校验值。


#### OCR 候选核对关闭与 P0 组件更新

上述 455 对候选现已全部完成模型辅助视觉核对：35 对逐对检查，另外 420 对按外部图片分组，查看全部 161 张外部图片并对照相应转写。两批共涉及 167 张不同外部图片，均未判断为视觉重复。`0e53f27` 新增核对记录的完整性检查，逐项绑定候选内容、原图／RGB 与父报告校验值；缺项、重复项、真实冲突和未决结果不能关闭。结果仅表示这份固定候选列表已核对、已知分组关系未跨划分；人工来源质量审核、语义改写和外部 benchmark 排除仍待完成。原始图片、复核样本与决定明细保留在忽略目录，公开快照保存方法、数量和校验值。

首批四片 OCR 已接入 MF1 的 `pretraining-mf1-p0-components-v6`。组合包含 **670,540 条训练记录、594,222,689 CE、136,705 张独立训练图片**；其中 OCR／文档 **4,251,138 CE**。按既定 P0 比例，caption、OCR／文档、VQA 仍分别缺 **9,247,247／9,748,862／5,299,202 CE**。组合只新增约 **2.49 MB 元数据**，不复制媒体或 token payload。新增留出样本后重新固定验证窗口：常规 **1,000,191 CE／2,179 个窗口**，阶段末 **5,000,872 CE／10,954 个窗口**；这是验证库存与选择结果，尚未执行模型评估。成员和校验值已更新，不能继续沿用 v5 的选择身份。

同一提交修正后续 OCR 生成方式：原始文本组、精确图片身份和已留出来源继续保留，排版 pHash 关系延后到使用实际转写的跨池检查。原先四片保持不变。新增 12 个不相交原文区间，四路 CPU 顺序构造，输出总上限 **48 GiB**、写入保留 **80 GiB**；观察时 **8 片完成、4 片运行、0 片排队**。已关闭的 04–07 片同时进入三套模型编码，共 12 个任务、最多三路 CPU 编码，合计上限 **3 GiB**。这些新片均需新的跨池核对，不能继承首批四片的通过结论。按首批估算的 OCR 供给仍不足 P0 目标，需补充独立来源，不能靠重复渲染凑数。

相关 **29 项**检查通过，完整 CPU 回归 **495 通过、1 跳过、54 项 CUDA 未运行**，耗时 **153.84 秒**；185 个源码文件类型检查与全仓静态／格式检查通过。辅助工作站自然图片增量记录 **30,750 张、57,579 条 QA**，仍在构造。完整绑定与带观察时间的运行快照见[媒体库存](media-inventory.json)。四模型正式主 CE 仍为零；正式 TensorBoard **6008** 的首页、run 列表与标量接口均返回正常，尚无正式训练事件。


#### 原生多来源读取与后续 OCR 供给

`dceac57` 让来源模型的原生读取器在同一个入口消费多份媒体编码，共享文本只计一次，各组件保留自己的像素目录、索引和身份。检查覆盖组合前后 token／labels／媒体张量一致、跨来源组合、共享文本不重复、混合采样精确恢复，以及重复样本、跨划分图片、不同配置和文件变更的拒绝路径。完整 CPU 回归 **501 通过、1 跳过、54 项 CUDA 未运行**，耗时 **159.45 秒**；全仓静态、格式和 186 个源码文件的类型检查通过。实际接入自然图片时，首次执行因任务修正审计使用 `encoded_manifest_sha256` 字段而被拒绝，未产生组合目录；`8a940cd` 补齐已有审计格式的兼容读取，新增过滤后组合检查，相关 **19 项**通过。两次失败记录保留，源数据与通过标准未改写。

Kimi K1、Qwen Q1 均已组合现有自然图片和最初四片 OCR，引用同一份 64K 文本编码。每条组合的原始训练记录为 **516,904**，其中媒体 **118,097** 条、独立 RGB **98,103** 张；1024 长度下可消费 **552,265,096 CE**。两个组合含固定验证选择各仅新增约 **5.53 MB 元数据**。实际读取器在 512 长度下整条排除超长媒体：Kimi 可消费 **552,224,519 CE**，Qwen **552,242,516 CE**；两者在 1024 长度下均与完整编码库存逐计数相符。文本切成窗口后，读取器样本数不同于原始文档数，两个口径已分别记录。

两模型的验证库存均支持固定、无重复的 1M／5M CE 选择；512 长度对应 **1,000,286／5,000,380 CE**，1024 对应 **1,000,862／5,000,299 CE**。同一长度下二者验证成员一致；Kimi／Qwen 图像处理仍各用自己的实现与 feature 数。这是数据消费与验证集合核算，没有执行模型效果评估。当前组合的验证图片仅 **823 个独立 RGB**，尚未满足至少 1,000 个视觉评估组的要求；文档／图表编码、新 OCR 和自然图片增量仍需接入。

新增十二片 OCR 原图已全部关闭，加上首批共 **308,250 张图片／条转写**，训练 **302,190**、验证 **2,892**、测试 **3,168**；各片源文本划分不一致数为零。原图合计 **5,192,448,222 字节**。扩展检查复用六份已有身份导出，只新建十二份导出，合计输出上限 1 GiB，覆盖 **371,500 张唯一 RGB**（含既有自然图片和文档／图表）。十六片的三套编码 **48/48 全部完成**；下列 CE 为随后隔离之前的库存。MF1 全部十六片编码已完成，训练候选 **11,167,680 CE**；加上既有文档组件为 **12,639,190 CE**，距离 P0 OCR 目标仍缺 **1,360,810 CE**，需补充有独立来源的内容。

辅助工作站的自然图片增量已关闭，新增 **36,740 张图片、68,856 条 QA**，后续仍需修正任务分类、跨片分组及模型编码。产物、校验值、验证选择、磁盘余量及带观察时间的运行状态见[媒体库存](media-inventory.json)。人工质量审核、外部视觉污染核查、tokenizer 冻结、阶段供给及生产资格仍未完成；四模型正式 CE 均为零，6008 正式看板仍无训练事件。

首次失败与后续成功的记录继续分别保存。台账读取已修正：后续重试发布到同一目标目录时，不得将其成功状态或数据量归给首次失败任务；相关 **14 项**检查通过。


#### 共享文本近重复漏检与训练组隔离

扩展检查发现 OCR-06 的一个训练组与 OCR-07 的测试组相连。实际转写在去除空白、规范化并 casefold 后，五字符片段 Jaccard 为 **0.8615**；追查两篇父文本，相似度为 **0.9775**。父文本的 Simhash 距离为 **4**，且已命中一个相同索引段，但旧去重在距离超过 **3** 时跳过了 Jaccard 比较。这证明旧候选检索加距离门槛存在漏检，不能把已有编码机械检查解释为完整近重复排除。四个模型的共享文本消费者均受影响。

`c9f1cdd` 复用已有分组视图机制，增加文本训练组隔离。已生成 `pretraining-text-partition-v3`，排除 **1 条训练父文本**；同时生成 `pretraining-ocr-p0-shard-06-v2`，排除其 **1 条 OCR 派生样本**。原始数据库保持不变，既有验证／测试成员及所属划分的校验值不变。两份新视图合计新增约 **15.41 MB 元数据**；文本训练记录变为 **398,806**，该 OCR 片训练记录变为 **18,794**。相关分组路径 **54 项**通过，最新完整 CPU 回归 **505 通过、1 跳过、54 项 CUDA 未运行**，耗时 **154.13 秒**；186 个源码文件类型检查和全仓静态／格式检查通过。

这只处置了已发现的一对，**共享文本的完整训练／留出近重复核查尚未完成**。旧文本编码、OCR-06 编码和模型组合仍引用隔离前的成员，需要更新后重新绑定；tokenizer 训练成员也要核对。前述 CE 与固定验证选择均保留为隔离前的机械库存记录，不能用于宣布正式准入。扩展媒体检查另有 **1,899 对**视觉候选；其中 **455 对**与旧候选内容完全一致，可在明确重绑定后复用既有决定，另外 **1,444 对**仍需核对。证据和当前状态写入[文本库存](text-inventory.json)与[媒体库存](media-inventory.json)，原文与样本图片不进入 Git。四模型正式训练仍未启动。


#### 文本精确核查与自然图片增量分类

`36a40a8` 增加共享文本的全量跨划分核查：验证／测试建索引，训练文本逐篇流式读取；使用去除空白、规范化并 casefold 的五字符片段，按 **Jaccard ≥ 85/100** 精确比较。候选采用共同顺序的前缀过滤，整数编号与原始片段一一对应，不使用哈希碰撞或 Simhash 距离作为通过条件。方法依据 [Xiao 等的 WWW 2008 论文第 3 节](https://www.cse.unsw.edu.au/~lxue/WWW08.pdf)，实现与穷举集合比较，并用可公开的合成文本复现距离 4 的漏检。检查涵盖 train/val、train/test、val/test；不据此宣称同划分内部全部去重、语义转述或 benchmark 污染已排除。

新增核查与已有视图共 **20 项**检查通过。完整 CPU 回归 **513 通过、1 跳过、54 项 CUDA 未运行**，耗时 **184.17 秒**；187 个源码文件的类型检查、全仓静态检查与 335 个文件格式检查通过。已按冻结源码启动 `pretraining-text-partition-v3` 的完整核查，范围为 **398,806 篇训练、3,735 篇验证、4,392 篇测试**。单次时间上限 1 小时、常驻内存 24 GiB、报告 16 MiB，保留 80 GiB 空间，不使用 GPU、不产生训练 CE。

首个处理尝试在扫描 **18,000 篇训练文本**后主动停止：实际候选密度预示原来的 20M 候选／5M 精确比较上限会提前用尽。该未完成记录和停止依据保留；显式重试一次，将计数上限改为 **100M／18M**，算法、数据、相似度阈值及时间／内存／空间上限保持一致。首个尝试已经记录 **10 对**匹配，包含验证／测试冲突，但这只是部分扫描结果；重试完成前不发布完整通过结论，也不将旧编码升级为正式数据。之后须处理全部训练冲突与验证／测试重叠，再同步文本编码、OCR 派生样本和模型组合。

已重建两个 tokenizer 共同使用的 **10,880 篇、67,107,100 字节**训练成员，字节校验值与原 BPE 报告完全一致。最先隔离的父文本不在该集合中；完整核查的新剔除组尚待比较，所以词表仍未冻结，没有重复执行 BPE 训练。

辅助工作站新增自然图片的任务分类已完成：复用已验证、固定版本的 ALLaVA 15 个 caption 指令规则，形成 `pretraining-natural-extension-partition-v1`。原图、回答、样本、组和划分逐项保持，训练分成 **33,711 条 caption、34,093 条 VQA**；验证为 **154／151**，测试为 **373／374**。新视图仅 **891,648 字节**，另导出 **15,185,512 字节**身份信息到本机；约 28.58 GB 原图继续共享远端存储。跨池分组、质量／污染核查和三个模型的编码仍待完成。具体校验值、停止记录、当前扫描量及观察时间见[文本库存](text-inventory.json)和[媒体库存](media-inventory.json)。四模型正式 CE 仍为零，正式 TensorBoard 6008 已正常运行，尚无训练事件。
