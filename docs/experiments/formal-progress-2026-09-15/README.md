# 正式预训练进度 · 2026-09-15

观测时间：**2026-09-15 16:46 UTC**。六个训练进程均在运行；本页是固定快照，不随训练刷新。原始计数、验证选择标识和相对输出路径见 [progress.json](progress.json)，后续安排见[基础预训练计划](../../pretraining-plan.md)。

## 当前阶段

M 表示百万 token。阶段进度按本阶段有效 CE 计算；累计主 CE 包含已完成的主训练阶段，不包含索引器 input token。step 是当前阶段的更新计数，不能跨模型比较。

| 模型 | 阶段 | step | 本阶段 CE / 预算 | 阶段进度 | 累计主 CE |
|---|---|---:|---:|---:|---:|
| MiniKimi-K3 | K2 | 14,350 | 446.17M / 1,200M | 37.18% | 646.19M |
| MiniQwen4 | Q2 | 8,160 | 132.04M / 1,200M | 11.00% | 432.06M |
| MiniDeepSeek-V4 | D4 | 2,030 | 67.98M / 1,500M | 4.53% | 818.02M |
| MiniFrontier1.0 | P1 | 27,657 | 374.90M / 800M | 46.86% | 574.91M |
| MiniDeepSeek-V4.1 | D2 | 695 | 23.28M / 500M | 4.66% | 273.30M |
| MiniFrontier1.1 | P0 | 10,521 | 144.14M / 200M | 72.07% | 144.14M |

DeepSeek-V4 的 D3 已完成 299 次更新、10,031,719 input token，随后进入 D4 稀疏预训练。V4.1 的 D1 已完成 250,027,355 CE，随后进入 D2。六版本完整计划共 16B 主 CE；当前阶段百分比不代表整个模型的训练完成度。

## 固定验证

以下是观测时日志中最近一次周期验证，约 1M CE；验证发生在表列的累计主 CE 位置，不一定对应当前 step。不同 tokenizer、验证选择及训练阶段的 NLL 不直接排名。

| 模型 | 验证时累计主 CE | NLL | 说明 |
|---|---:|---:|---|
| MiniKimi-K3 | 600.00M | 3.3154 | K2 周期验证 |
| MiniQwen4 | 400.00M | 3.6207 | Q2 周期验证 |
| MiniDeepSeek-V4 | 800.02M | 3.0747 | D4 周期验证 |
| MiniFrontier1.0 | 550.00M | 3.1759 | P1 周期验证 |
| MiniDeepSeek-V4.1 | 250.03M | 3.3181 | D2 step 0 起点评估，尚不反映 D2 更新效果 |
| MiniFrontier1.1 | 100.01M | 4.0223 | P0 周期验证 |

[9 月 14 日检查点诊断](../checkpoint-diagnostics-2026-09-14/README.md)覆盖原四模型当时保存的 13 份权重，共 1,949 条生成。固定权重存在严重重复；该批次没有测试上述最新进度的权重，也没有覆盖 V4.1 或 MF1.1。周期验证与短续写诊断回答不同问题，尚不能据此认定基础预训练质量合格。

## 执行变更与待办

Qwen 已采用按专家负载分桶的执行路径：相同数据窗口的前向与反向耗时从 12.141 秒降至 9.206 秒，对应吞吐增加 31.9%。这一测量不含优化器更新、验证和保存，也不代表所有数据窗口的提速。MF1.1 的编译 mHC 候选因梯度差异未采用，正式训练保留参考实现和梯度检查点。数值误差、测量条件及恢复依据见[执行审计](../../audits/training-infrastructure.md)。

Kimi K2、DeepSeek-V4 D4 和 V4.1 D2 在启动 OOM 后降低 microbatch，从各自完整父检查点恢复；全局 input 目标与阶段预算保持。MF1 P1 曾因缓存保留峰值守卫暂停，已从完整检查点恢复，仍保留 22 GiB 守卫。最近的执行调整均有单独记录，本次整理仅读取日志与进程状态。

接下来需发布 Kimi K3、Qwen Q3、MF1 索引器阶段及 MF1.1 P1 的队列项。兼容数据视图已经准备，不代表这些后续阶段已经入队。生成重复的训练原因仍需结合数据消费、EOS/loss mask 和同模型固定评测继续定位。

观测时磁盘空闲 103.07 GiB，正式训练保留线为 80 GiB。JSON 中的吞吐为最近最多 20 条日志更新速率的算术平均，排除了验证、保存及等待；本页不据此推算全程完成时间。

实验台账的审核告警尚未清零。另查当日 14:12 UTC 台账快照，MF1 P1 的重复运行告警来自被扫描的旧控制器档案；旧进程已退出，当前只有一个 P1 worker。六个当前正式输出的 review 登记仍未覆盖，本页的进程与日志核对不代表台账已完成全部审核。

## 更新方式

在训练主机仓库根目录运行以下只读命令，可查看当时的日志、进程与存储状态；它不加载权重或启动训练：

```bash
python -m scripts.training_status --run formal
```

本记录只分发汇总数据；训练输出、检查点和完整诊断日志保留在本地。
