# v0.1.0 研究预览版

状态：仓库提供研究预览源码，尚未创建正式 Release 或上传模型权重。

首版提供三条来源可追溯的对照架构，以及融合主线 [MiniFrontier1.0](../models/minifrontier1.md)、离线微型训练示例、训练与推理工具、源码对照测试和轻量实验档案。完整数十亿 token 主训练、经过验收的聊天/视觉模型、多教师正式后训练及草稿加速结果属于后续工作。

## 能力状态

| 范围 | 已实现 | 已验证 | 已训练／待完成 |
|---|---|---|---|
| MiniFrontier1.0 融合主线 | 是，独立原生多模态 decoder 与 mf1 流程 | CPU 数值、完整配置前后向、恢复、打包、媒体、草稿与 wheel | 小规模机制学习；3090 profiling、正式数据/训练/能力待验收 |
| 三条文本架构 | 是 | 源码组件、梯度、因果性、CPU/DDP；覆盖范围见模型页 | 诊断与小规模配方试验；完整主训练未完成 |
| 原生视觉与 MTP | 是 | 短流程、数值和掩码测试 | 小型诊断；正式视觉分布及 MTP 配方未验收 |
| 三个来源模型的 Muon 与路由更新 | 是 | 独立数值/更新测试 | 优化器与学习率仍在比较 |
| Kimi / DeepSeek QAT 数值仿真 | 是 | 数值与更新测试 | 尚无训练后量化部署结果；Qwen 未声明该配方 |
| 原生缓存与草稿路径 | 是 | 增量一致性、接受/拒绝和回滚正确性 | 未获得训练后加速结论 |
| 工具 rollout、GRPO/MOPD/OPD | 是 | 工程和可核验任务测试 | 9/12 教师资格与正式 RL 预算待完成 |
| 浏览器 demo | 原三模型文本界面；MF1 独立多媒体诊断页 | 权重发现、能力门槛及 MF1 媒体预算/API 检查 | 无通过能力验收的聊天权重 |

“已实现”是代码存在；“已验证”必须有对应范围的检查；“已训练”只描述实际预算和数据，不自动表示具备能力。模型清单的 `training_ready` 表示训练入口可执行，需结合 `training_ready_scope` 和 `usable_chat_checkpoint` 阅读。

## 分发支持范围

| 操作 | Git checkout / editable install | wheel |
|---|---|---|
| models、离线 quickstart、显式配置 acceptance 训练、显式 checkpoint CLI 生成 | 支持 | 支持，单独在 checkout 外验收 |
| 原始源码 oracle、项目测试、数据研究与调度脚本 | 支持 | 不包含完整开发资源；使用 checkout |
| 正式策略检查与训练 | 需要干净且可追溯的 Git 源码、原策略正文、数据与前驱证据 | 首版不支持；入口报告需要 Git checkout |
| 默认浏览器体验 | 需要独立验收后的权重 | 同样需要独立权重；包内不附带模型 |

sdist 保留开发资源与历史研究材料，但解包的 sdist 本身没有 Git 身份，也不自动满足正式策略训练。wheel 中的策略 JSON 仅为参考资源，不能单独代替方案正文与 Git 证据。完整训练请从正式仓库 checkout 后 `uv sync --locked` 或 editable install。

## 许可与维护信息

整个分发包声明 `Apache-2.0 AND MIT AND LicenseRef-Kimi-K3`；这不改变每个文件原有条款。Kimi 派生组件保留完整自定义许可及其商业使用条件。组件对应关系见[第三方说明](../../THIRD_PARTY_NOTICES.md)，数据与后续权重单独核定。

代码仓库为 [576469377/MiniFrontier](https://github.com/576469377/MiniFrontier)，文档直接维护在[仓库 docs](../README.md)，问题反馈使用 [Issues](https://github.com/576469377/MiniFrontier/issues)。

安全/行为问题的私下联系渠道仍待维护者提供，正式版本发布前需同步补齐 SECURITY 和行为准则。当前不声明已启用 GitHub 私密安全报告或独立文档站。

## 路线图

1. 完成优化器、学习率、MTP、tokenizer 和补种子试验，冻结正式配方与数据准入。
2. 以 MiniFrontier1.0 为融合主线推进训练与独立语言/视觉能力评估，保留三条来源架构对照。
3. 完成合格教师、多教师后训练、正式 RL 及目标绑定的草稿训练/速度评估。
4. 发布权重时提供完整模型卡、tokenizer、配置、校验值、能力评估、局限与产物许可。
5. 扩展 wheel 的正式策略资源支持及更完整的浏览器体验。

首次公开材料包括失败与未完成结果；不把损失下降、阶段退出或诊断记忆成功写成模型能力通过。

本地检查范围记录在[预览验收记录](preview-validation.json)，最小示例的实际输出另存于实验档案。CI 配置覆盖 Python 3.11/3.13 的静态检查、CPU/Gloo、构建和安装包验收；每次提交的实际执行结果见 [GitHub Actions](https://github.com/576469377/MiniFrontier/actions)。
