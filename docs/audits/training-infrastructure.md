# 训练执行优化（2026-09-11）

2026-09-11 的执行优化降低了数据等待与 Qwen 优化器开销，并修复 Kimi 恢复后的闲置显存占用。正式任务从原检查点接续，模型结构、样本窗口、学习率和损失分母保持；数值差异与源码切换依据见下文。当前训练安排见[预训练计划](../pretraining-plan.md)。

## 数据通路

- `MINIFRONTIER_PREFETCH_WINDOWS=1` 启用一个 CPU 后台线程，最多提前准备一个训练窗口。默认关闭。
- 生产线程独占训练数据集与采样器，使用采样器自身 RNG；检查点仅保存已消费窗口的采样状态。预取但未消费的内容在恢复后重新准备。
- 原生模型入口仅支持单卡、固定 input-token 目标、sample-bounded 的预训练。MF1 限于采样随机数完全由生产线程管理的 pilot/P0/P1/P3；P2 不启用此路径。
- 媒体缓存按固定数量的锁条带协调下载，同一文件合并请求；远端下载不再持有整个缓存的排他锁。原始字节 SHA256、缓存容量、验证图片 pin 和磁盘预留仍保留。
- Python 未暴露 `os.memfd_create` 时尝试 libc 接口，为原处理器提供相同字节的匿名内存文件；系统不支持时仍回退到有界临时文件。

预取将原始数据准备与模型计算重叠，视觉编码器仍逐步训练。共享磁盘预留锁保留，检查点写入仍可能使其他任务等待。

## Qwen 优化器

同形状的语义矩阵块最多 32 个合批执行 Polar Express。每个矩阵独立做 Frobenius 归一化，仍使用原来的 FP32、8 轮系数、epsilon、动量、缩放和权重衰减。混合 Q/gate 中的 AdamW 行及 n-gram 表的衰减规则不变；检查点中的参数组与状态布局不变。

在单张 RTX 3090 上，以 513,405,536 参数配置的同一份正式检查点及同一组固定梯度，分别执行旧、新优化器各 3 次更新。第 1 次用于稳定执行，第 2—3 次取平均；没有增加正式训练 token。

| 项目 | 逐矩阵执行 | 同形状合批 |
| --- | ---: | ---: |
| 优化器平均耗时 | 3.664 秒 | 0.498 秒 |
| 峰值 allocated 显存 | 6.806 GiB | 6.804 GiB |

[逐次数值记录](training-infrastructure.json)包含梯度 seed、配置校验值和源码版本。优化器部分约快 7.36 倍。3 次更新后的参数最大绝对差异为 `4.47e-8`，相对实际参数更新的 L2 误差为 `3.12e-6`；优化器动量及 Adam 状态逐值一致。矩阵合批改变浮点舍入，因此结果按容差比较。

## 验证与恢复

针对优化器、缓存并发/损坏/容量、预取、训练运行时和初始阶段绑定的 CPU 检查共 27 项通过；另有 2 项启用预取的规范文本/图文恢复检查通过。MF1 的串行、预取和暂停恢复对照保持模型、优化器、路由、采样器、token 台账与 RNG 逐值一致。

源码切换需要显式的 `execution_only_resume` 记录，绑定原检查点 SHA256、新旧源码身份、阶段证据和验证记录。恢复比较仍拒绝 batch、数据、模型配置、优化器、预算或阶段变化。原检查点和原运行证据保留，后续日志使用新源码身份。

本轮单卡任务支持在输出目录创建 `pause.request`，于当前优化器更新完成后保存并暂停。

## 正式接续后的观察

三个任务接续后的早期记录如下。各行来自实际混合数据，批次与观测窗口不同。

| 模型 | 观测训练步 | 已记录步数 | CE token/s | 前台数据等待 |
| --- | --- | ---: | ---: | ---: |
| Kimi | 405–430 | 6（每 5 步记录） | 约 3,756 | 约 0.006 秒/步 |
| Qwen | 305–365 | 13（每 5 步记录） | 约 1,456 | 约 0.002 秒/步 |
| MF1 | 652–671 | 20 | 约 1,807 | 约 0.035 秒/步 |

启用预取后，`data_preparation_seconds` 表示前台等待数据及尚未前移的元数据准备，不再包含与 GPU 计算重叠的全部后台准备时间。应结合完整更新耗时判断预取收益。[数值档案](training-infrastructure.json)保留逐条记录、seed、配置/数据/tokenizer 校验值与实际源码版本。

Kimi 两次在恢复后的第 401、402 步触发 2 GiB 空闲显存保护，完成的更新均先保存了检查点。仅设置分配器自动回收阈值没有解决；后续版本在验证结束以及必要的预留检查前显式释放闲置 CUDA 缓存，恢复后已继续更新。该操作只回收闲置缓存，见 [PyTorch 内存管理说明](https://docs.pytorch.org/docs/2.14/notes/cuda.html#memory-management)。microbatch、全局 batch、计算公式与 2 GiB 预留均保持原值，峰值计数继续保留。Qwen/MF1 使用 `9b1d8fd`，Kimi 使用包含此修正的 `4ba8955`。

## TensorBoard 展示

四个模型共用 `train / eval / perf` 分类。`train` 展示已记录的训练损失、梯度、学习率与进度；`eval` 保留总体验证损失、分域损失和对应 token 分母，阶段末验证使用 `phase_end_` 前缀；`perf` 展示吞吐、步耗时、数据等待、优化器耗时和显存。9 月 11 日初版保留各训练器的吞吐名称，显存统一使用 GiB；后续命名调整见下文。

启动、QK 裁剪和缓存回收等运行事件完整保留在 `metrics.jsonl`，不再将这些事件附带的累计 token 数重复画成学习曲线。旧版 Kimi 的 `qk_clip/main_ce_tokens` 只是累计计数，不是 QK 裁剪幅度；逐层诊断一直保存在 JSONL 的 `layers` 中。预训练验证中的 reward 占位值、布尔状态也不再生成图表。

展示服务使用 [展示同步脚本](../../scripts/sync_mf1_tensorboard.py) 从原日志构建独立 event 目录，并保留原 step 和 wall time，再切换 TensorBoard 展示目录。脚本名称为兼容已有服务保留，支持全部训练器；`--watch --event-driven` 在 Linux 上由文件写入事件唤醒，`--interval` 控制更新合并间隔。每次启动使用新的展示目录。源日志被替换或截断时明确报错，需在新目录重建视图。

5 项 CPU 展示检查通过，覆盖分类、原生与 MF1 分域指标、原始时间戳、半行写入、增量同步及展示链接保护。本次只更换展示服务，正式训练进程未重启。

### 2026-09-13 显示完整性修复

逐步对照确认源日志和同步 event 没有历史缺段。页面默认每条曲线采样 1,000 点，MF1 P0 的 14,599 个训练记录因此只返回 1,000 点。展示上限提高到 100 万后，HTTP 返回全部 14,599 点，首末 step 为 1 和 14,599；DeepSeek D1、Kimi K1 也分别返回完整的 1,509、1,295 个已记录点。

同步视图补齐原生训练器的嵌套 token 计数，并按已记录的训练阶段统一 CE 吞吐与验证分母。模型未记录的指标不补造。正式看板移除失败运行的展示链接，原始失败证据保留；预登记的 Q2 通过目录创建事件自动接入。34 项 CPU 展示检查通过，另有 3 项目录和文档检查通过。部署只重启监控服务，四条正式训练进程继续运行。操作方式见[实验管理](../operations/experiment-management.md#tensorboard)。

## 2026-09-14 六模型执行优化

本次覆盖 MiniKimi-K3、MiniQwen4、MiniDeepSeek-V4、MiniDeepSeek-V4.1、MF1 和 MF1.1。修改的是执行方式：模型结构、参数顺序、损失分母、可见性掩码、路由频率、优化器算法、数据与阶段预算保持。六个模型均已完成执行切换并持续训练，Qwen 已保存第 1200 步常规检查点；恢复后的初步观测见下文。

首批改动为 `829a8bd`，五个模型使用执行快照 `3fd51a6`；快照保留了原先绑定的 07 号策略正文和 V4.1 计划。Qwen 随后使用包含 GDN 优化的 `c4a5822`。各任务继续使用检查点绑定的实际 batch、学习率与数据。完整版本和数值记录见[审计 JSON](training-infrastructure.json)的 `execution_2026_09_14`。

### 修改范围

- **预取与恢复**：适用的正式预训练默认启用一个 CPU 预取窗口，修复后续阶段因遗漏环境变量而退回串行的问题。原生入口仍要求单卡、固定 input-token 目标、sample-bounded 与可恢复混合采样；MF 限于 pilot/P0/P1/P3，P2 保持串行。可用 `MINIFRONTIER_PREFETCH_WINDOWS=0` 关闭。已消费窗口的采样器状态仍是恢复依据。原生 `--resume` 已完成更新且沿用按 token 验证时，不重复起点验证；新阶段与阶段末验证保留。
- **Qwen**：路由频率预计算保留主干、MTP 解码与路由收集，省去无人使用的词表损失。CUDA dense 预训练使用 SDPA，保留因果、padding 与 MTP 掩码；QSA 阶段继续使用需要返回注意力概率的路径。专家循环一次读取活跃专家列表。
- **DeepSeek-V4**：每个查询块只裁掉对该块所有查询都不可见的键，图像自定义可见性仍保留完整支持。mHC 用 FP32 矩阵收缩替代五维广播中间量。
- **Kimi**：稳定归组路由索引，取消逐专家的 CUDA `nonzero` 同步；专家调用、token 顺序、累加顺序和空专家的零梯度保持。视觉网格与分段信息在层外一次准备。
- **MF1/MF1.1**：各自的视觉实现一次准备整数元数据，RoPE 在单次调用中复用相同角度的 sin/cos。V4.1 与 MF1.1 接续使用此前已验证的本地单程 mHC FP32 收缩，参数与公式保持。

### 切换前基线

每行取切换前最后 100 条训练记录的中位数。各模型的文本长度、视觉占比和 microbatch 数不同；CE token/s 不包含 padding 或图像特征，不能直接与纯文本性能探测比较。

| 模型 / 阶段 | 记录步范围 | CE token/s | 更新耗时 | 前台数据准备 |
| --- | --- | ---: | ---: | ---: |
| MiniKimi-K3 / K2 | 6270–6765 | 2,112 | 14.70 秒 | 1.362 秒 |
| MiniQwen4 / Q2 | 650–1145 | 762 | 20.97 秒 | 0.748 秒 |
| MiniDeepSeek-V4 / D2 | 12060–12555 | 2,691 | 12.42 秒 | 0.010 秒 |
| MiniDeepSeek-V4.1 / D1 | 435–930 | 2,679 | 12.42 秒 | 0.011 秒 |
| MF1 / P1 | 14404–14503 | 1,765 | 7.70 秒 | 1.314 秒 |
| MF1.1 / P0 | 760–859 | 1,217 | 11.26 秒 | 1.456 秒 |

### 算子对照与数值边界

以下为 RTX 3090 上的 BF16 前向加反向短测，没有增加正式训练 token。每种实现先稳定执行，再计时 2–5 次；具体 seed、shape、重复次数及显存口径保留在 JSON 中。

| 算子 | 修改前 | 修改后 |
| --- | ---: | ---: |
| Qwen dense attention | 33.10 ms | 13.69 ms |
| DeepSeek-V4 attention，压缩比 0 / 4 / 128 | 61.11 / 71.10 / 59.75 ms | 37.51 / 44.07 / 39.77 ms |
| DeepSeek-V4 mHC 输出收缩 | 7.65 ms | 5.90 ms |
| Kimi 路由，8,192 token、32 专家 | 53.03 ms | 46.44 ms |

Qwen attention 的增量峰值显存从 4,290.9 MiB 降至 558.9 MiB；DeepSeek-V4 mHC 从 1,217.0 MiB 降至 576.3 MiB。视觉三项的耗时差异在约 6% 内，MF1.1 未测得加速。这些是算子结果，不能按同一倍率推算整段训练收益。

Kimi 路由和三项视觉测试的输出、输入梯度及全部参数梯度逐位一致。Qwen 的 SDPA 与 DeepSeek 的注意力、mHC 收缩保持公式和掩码，但浮点归约顺序不同；上述首批算子对照的最大相对 RMS/L2 差异小于 `0.007`，按容差通过，不能称为逐位一致。

105 项 CPU 聚合回归、Ruff 和相关源码 mypy 通过，覆盖预取恢复、路由频率与损失、注意力可见性、梯度和模型工作流。六份模型配置及优化器参数组顺序、四份原生训练阶段绑定均与原任务核对一致。测试入口见 JSON 的 `verification.test_files`。

### Qwen GDN 与恢复控制补充

Qwen 的第二次执行升级为 `c4a5822`，从 Q2 第 1186 步的完整检查点恢复；其余五个模型继续使用 `3fd51a6`。支持的 CUDA BF16 无缓存路径改用 FLA GDN chunk 内核，保留投影、卷积、Q/K 归一化、门控、head 映射、padding 和 FP32 递归状态。CPU、缓存推理及不支持的输入仍走原 torch 路径。

在相同 RTX 3090、`B=8 / S=2048 / H=512` 下，单个 GDN 层的前向加反向中位耗时从 **85.01 ms 降至 13.49 ms**，增量峰值显存从 **1,499.7 MiB 降至 625.2 MiB**。每种内核预热 2 次、计时 3 次；包括首次编译的整个检查耗时 82.1 秒。约 6.3 倍是单层结果，不能当作整步训练提速。

测试覆盖梯度检查点重算和非零 FP32 初始、最终状态。输出相对 L2 差异约 0.357%，整体参数梯度约 0.44%，最差单参数梯度约 0.871%；输出、状态与梯度均有限，按容差通过。另有 11 项 CPU 检查、Ruff 和 mypy 通过，入口为 [GDN 后端测试](../../tests/test_qwen_gdn_backend.py)。本次验证没有消耗正式训练 token；恢复后的整步观测见下表。

切换期间，控制脚本清理暂停标记的顺序存在竞态，导致两个 MF 新进程各完成一次更新后再次暂停。完整检查点均已保存，没有丢失更新。清理顺序修正后，MF1 与 MF1.1 分别从第 14602、934 步继续；这属于本机执行控制问题，未改变模型或训练器计算。

### 六模型正式恢复后的初步观测

截至 2026-09-14 13:03 UTC，六个模型都在连续更新。以下为正式日志中位数：切换前各取 100 条记录，切换后最多取 50 条。前后使用不同的实际数据窗口，因此属于初步运行观察，不是严格的端到端性能对照。Qwen 在 GDN 切换后目前仅有 **3 条记录**，不能据此估计长期稳定吞吐。

| 模型 / 阶段 | 恢复后记录步范围 | 记录数 | 切换前 CE token/s | 恢复后 CE token/s | 恢复后更新耗时 | 前台数据等待 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| MiniKimi-K3 / K2 | 6820–6905 | 18 | 2,112 | 2,430 | 12.76 秒 | 0.0062 秒 |
| MiniQwen4 / Q2 | 1190–1200 | 3 | 762 | 1,171 | 13.84 秒 | 0.0022 秒 |
| MiniDeepSeek-V4 / D2 | 12635–12740 | 22 | 2,691 | 3,114 | 10.71 秒 | 0.0054 秒 |
| MiniDeepSeek-V4.1 / D1 | 1020–1130 | 23 | 2,679 | 2,952 | 11.23 秒 | 0.0044 秒 |
| MF1 / P1 | 14670–14719 | 50 | 1,765 | 2,184 | 6.16 秒 | 0.0239 秒 |
| MF1.1 / P0 | 963–1012 | 50 | 1,217 | 1,385 | 9.88 秒 | 0.0005 秒 |

预取后的数据时间仅反映前台等待，不包含已经与模型计算重叠的后台准备。Qwen 当前使用 `c4a5822`，其余五项使用 `3fd51a6`；原模型配置、优化器状态、采样状态与阶段预算继续保留。聚合 105 项 CPU 回归和 GDN 补充 11 项检查均已通过；短 GPU 对照中的 BF16 注意力与归约存在已记录的微小舍入差异，不能将本次执行切换整体称为逐位等价。
